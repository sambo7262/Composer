---
phase: 04-playlist-generation
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - app/templates/pages/chat.html
  - app/templates/partials/chat_message.html
  - app/templates/partials/playlist_card.html
  - app/templates/base.html
  - app/routers/api_chat.py
  - app/routers/pages.py
  - app/services/chat_service.py
  - app/models/playlist.py
  - tests/test_plex_playlist.py
autonomous: false
requirements: [PLAY-05, PLAY-06]

must_haves:
  truths:
    - "User sees a chat interface at / with input at the top and results flowing below"
    - "Mood preset buttons appear on empty state and disappear once conversation starts"
    - "Generated playlist appears as an inline card with drag-to-reorder and X-to-remove"
    - "User can push a named playlist to Plex via a button on the playlist card"
    - "After push, success message appears in chat and playlist is saved to history"
    - "Layout is mobile-first, single-column, portrait-optimized"
    - "Track count slider/input is near the chat input with default 20"
  artifacts:
    - path: "app/templates/pages/chat.html"
      provides: "Main chat page template with input, presets, messages area"
      contains: "hx-post"
    - path: "app/templates/partials/chat_message.html"
      provides: "User and AI message bubble partial"
      contains: "chat-message"
    - path: "app/templates/partials/playlist_card.html"
      provides: "Interactive playlist card with drag-drop, remove, push-to-Plex"
      contains: "x-sort"
    - path: "tests/test_plex_playlist.py"
      provides: "Tests for push-to-Plex functionality"
  key_links:
    - from: "app/templates/pages/chat.html"
      to: "/api/chat/message"
      via: "HTMX hx-post form submission"
      pattern: "hx-post.*api/chat/message"
    - from: "app/templates/partials/playlist_card.html"
      to: "/api/chat/push-to-plex"
      via: "HTMX hx-post on push button"
      pattern: "hx-post.*push-to-plex"
    - from: "app/templates/partials/playlist_card.html"
      to: "/api/chat/remove-track"
      via: "HTMX hx-post on X button per track"
      pattern: "hx-post.*remove-track"
---

<objective>
Build the complete chat UI (the app's primary interface per D-01) and push-to-Plex functionality. Chat page at `/` with input-at-top layout (D-02), mood presets on empty state (D-04), inline playlist cards with drag-reorder and remove (D-12), track count control (D-14), and push-to-Plex with history recording (D-15, D-16, D-17). Includes Alpine.js Sort plugin for drag-and-drop (PLAY-05) and PlexAPI playlist creation (PLAY-06).

Purpose: Delivers the complete user-facing experience -- the chat IS the home page. User can describe a mood, see a playlist, edit it, and push to Plex.
Output: Chat page template, message partials, playlist card with interactivity, push-to-Plex endpoint, visual verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-playlist-generation/04-CONTEXT.md
@.planning/phases/04-playlist-generation/04-RESEARCH.md
@.planning/phases/04-playlist-generation/04-01-SUMMARY.md
@.planning/phases/04-playlist-generation/04-02-SUMMARY.md
@.planning/notes/mobile-first.md
@.planning/notes/playlist-refinement.md
@.planning/notes/feature-ideas.md

@app/templates/base.html
@app/templates/partials/nav.html
@app/templates/partials/sync_banner.html
@app/routers/pages.py
@app/routers/api_chat.py
@app/services/chat_service.py
@app/models/playlist.py

<interfaces>
<!-- From Plan 01 and Plan 02 outputs -->

From app/services/chat_service.py:
```python
class ChatSession:
    session_id: str
    messages: list[dict]
    current_playlist: list[int]  # Track IDs in order
    track_count: int

def get_or_create_session(session_id: Optional[str]) -> ChatSession
def process_message(session_id: str, user_message: str, track_count: int, db_session: Session) -> dict
# Returns: {tracks: list[Track], explanation: str, criteria: FeatureCriteria, session_id: str}
def clear_session(session_id: str)
```

From app/routers/api_chat.py:
```python
# POST /api/chat/message - returns HTML partials (user bubble + AI response)
# POST /api/chat/remove-track - removes track from playlist
# POST /api/chat/reorder - reorders track in playlist
# POST /api/chat/new - starts new conversation
```

From app/models/playlist.py:
```python
class Playlist(SQLModel, table=True):
    id, name, mood_description, track_count, created_at
class PlaylistTrack(SQLModel, table=True):
    id, playlist_id, track_id, position
```

From app/templates/base.html:
```html
<!-- Alpine.js loaded at /static/js/alpine.min.js -->
<!-- HTMX loaded at /static/js/htmx.min.js -->
<!-- Tailwind CSS at /static/css/output.css -->
<!-- Main content area: <main class="max-w-[720px] mx-auto pt-12 px-4"> -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chat page template, message partials, and playlist card with drag-drop</name>
  <files>app/templates/pages/chat.html, app/templates/partials/chat_message.html, app/templates/partials/playlist_card.html, app/templates/base.html, app/routers/pages.py</files>
  <read_first>app/templates/base.html, app/templates/partials/nav.html, app/templates/partials/sync_banner.html, app/templates/pages/library.html, app/routers/pages.py, app/routers/api_chat.py, .planning/notes/mobile-first.md</read_first>
  <action>
1. **Update `app/templates/base.html`**: Add Alpine.js Sort plugin CDN script BEFORE the Alpine.js script (Sort plugin must load first per Alpine plugin convention):
   ```html
   <script defer src="https://cdn.jsdelivr.net/npm/@alpinejs/[email protected]/dist/cdn.min.js"></script>
   ```
   This goes before the existing `<script defer src="/static/js/alpine.min.js"></script>`.

2. **Create `app/templates/pages/chat.html`** (per D-01, D-02, D-03, D-04, D-05, D-14):
   Extends base.html. This is the app's PRIMARY interface.

   Layout structure (mobile-first, portrait-first per D-05):
   ```
   [Nav bar]
   [Chat input at TOP + track count control]  -- D-02, D-14
   [Mood presets row - empty state only]       -- D-04
   [Chat messages area - scrolls down]         -- D-03
   ```

   **Chat input section** (top of page, per D-02 -- NOT bottom like iMessage):
   - Full-width text input with placeholder "Describe a mood or vibe..."
   - Use `hx-post="/api/chat/message"` with `hx-target="#chat-messages"` and `hx-swap="beforeend"`
   - Add `hx-indicator="#chat-loading"` for loading state
   - Hidden input for session_id (generated on page load via `{{ session_id }}`)
   - Track count control: range input (slider) with min=5, max=50, step=5, default=20. Show current value next to it. Use Alpine.js `x-data="{trackCount: 20}"` with `x-model` for live display. The value is sent as hidden input `track_count`.
   - Submit button or enter-to-submit
   - Clear the input after submission: add `hx-on::after-request="this.querySelector('input[name=message]').value=''"` on the form

   **Mood presets** (per D-04 -- conversation starters, disappear once started):
   - Use Alpine.js: `x-data="{started: false}"` on a wrapper. The presets section has `x-show="!started"`.
   - After first message is sent (hx-on::after-request), set `started = true`.
   - Buttons: Chill, Energetic, Focus, Late Night, Workout
   - Each button fills the input and auto-submits (or directly posts via HTMX with the preset text as the message)
   - Style: horizontal row of pill-shaped buttons, wrap on narrow screens, muted colors that match dark theme

   **Chat messages area**:
   - `<div id="chat-messages" class="space-y-4 pb-8">` -- messages appended here by HTMX
   - Empty state: show a subtle prompt like "What are you in the mood for?" centered in the message area

   **Loading indicator**: `<div id="chat-loading" class="htmx-indicator">` with a spinner or "Thinking..." text

   **Pass context from pages.py route**: session_id (generate UUID on page load), ollama_configured (bool), active_page="compose"

3. **Update `app/routers/pages.py`**: The `/` route (modified in Plan 01 to render chat.html) needs to:
   - Generate a new session_id via `str(uuid.uuid4())`
   - Pass session_id, ollama_configured, active_page="compose" to chat.html template context
   - Import uuid at top

4. **Create `app/templates/partials/chat_message.html`**: Message bubble partial.
   - Accepts context: `role` ("user" or "assistant"), `content` (message text), `tracks` (optional list for playlist), `session_id`
   - User messages: right-aligned bubble, accent background, white text
   - Assistant messages: left-aligned bubble, surface-card background
   - If `tracks` is provided (assistant message with playlist): include the playlist_card.html partial below the explanation text
   - Use Jinja2 auto-escaping (default) for XSS prevention -- never use `|safe` on user content (T-04-07)

5. **Create `app/templates/partials/playlist_card.html`** (per D-12, D-15, PLAY-05):
   Interactive playlist card rendered inline in chat flow (D-03).

   Structure:
   ```
   [Playlist card header - track count, explanation]
   [Track list with drag handles and X buttons]
   [Push to Plex section - name input + button]
   ```

   **Track list with Alpine.js Sort** (per research Pattern, using Sort plugin):
   - `x-data` initializes with tracks array (passed as JSON from template context)
   - `x-sort` directive on the `<ul>` for drag-and-drop reordering
   - `x-sort:handle` on the drag grip icon (hamburger &#9776;)
   - Each `<li>` shows: drag handle, track number, title, artist, duration, X remove button
   - X button: `hx-post="/api/chat/remove-track"` with track_id and session_id, `hx-target="closest li"`, `hx-swap="outerHTML swap:0.3s"` (removes the `<li>` from DOM)
   - On reorder event: `hx-post="/api/chat/reorder"` with track_id and new position, `hx-swap="none"` (DOM already reordered by Sort plugin)
   - Touch-friendly: drag handles and X buttons at least 44px touch target (per mobile-first.md)

   **Push to Plex section** (per D-15):
   - Text input for playlist name (default empty, placeholder "Name this playlist...")
   - "Push to Plex" button: `hx-post="/api/chat/push-to-plex"` with session_id and playlist_name
   - `hx-target="#chat-messages"` with `hx-swap="beforeend"` to append success/error message
   - After successful push, button could change to "Pushed!" (use Alpine.js state)

   Style everything with Tailwind: dark theme, rounded corners, subtle borders, consistent with existing app aesthetic. Single column, no side-by-side panels (D-03, D-05).
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/Composer && python -c "
from jinja2 import Environment, FileSystemLoader
env = Environment(loader=FileSystemLoader('app/templates'))
chat = env.get_template('pages/chat.html')
msg = env.get_template('partials/chat_message.html')
card = env.get_template('partials/playlist_card.html')
print('All templates parse OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "hx-post.*api/chat/message" app/templates/pages/chat.html
    - grep -q "x-sort" app/templates/partials/playlist_card.html
    - grep -q "remove-track" app/templates/partials/playlist_card.html
    - grep -q "push-to-plex" app/templates/partials/playlist_card.html
    - grep -q "trackCount" app/templates/pages/chat.html
    - grep -q "Chill" app/templates/pages/chat.html
    - grep -q "alpinejs/sort" app/templates/base.html
    - grep -q "uuid" app/routers/pages.py
    - test -f app/templates/partials/chat_message.html
  </acceptance_criteria>
  <done>Chat page at / with input-at-top layout (D-02), mood presets on empty state (D-04), track count slider (D-14). Playlist card with Alpine.js Sort drag-drop reordering and X-to-remove (D-12, PLAY-05). Push-to-Plex button with name input (D-15). All mobile-first, single-column (D-05). Alpine.js Sort plugin loaded in base.html.</done>
</task>

<task type="auto">
  <name>Task 2: Push-to-Plex endpoint, playlist history recording, and integration test</name>
  <files>app/routers/api_chat.py, app/services/chat_service.py, tests/test_plex_playlist.py</files>
  <read_first>app/routers/api_chat.py, app/services/chat_service.py, app/models/playlist.py, app/services/settings_service.py, app/database.py</read_first>
  <action>
1. **Add push-to-Plex endpoint to `app/routers/api_chat.py`**:

   **POST /api/chat/push-to-plex** (per D-15, D-16, PLAY-06):
   - Accepts form data: `session_id` (str), `playlist_name` (str)
   - Validates: playlist_name is non-empty (strip whitespace) and under 200 chars (T-04-09), session exists, session has current_playlist with tracks
   - Gets Plex settings from settings_service (url, decrypted token)
   - Calls `push_playlist_to_plex()` (new function in chat_service.py) with:
     - Plex URL and token
     - playlist_name
     - List of plex_rating_key values for tracks in session.current_playlist
   - On success: Save playlist to history (D-17), return success message partial appended to chat
   - On error: Return error message partial with sanitized error (no Plex token leak per T-04-05)

2. **Add push_playlist_to_plex() to `app/services/chat_service.py`** (or create a helper function):
   - Implements the PlexAPI pattern from research (Pattern 5):
     ```python
     async def push_playlist_to_plex(plex_url, plex_token, name, rating_keys):
         def _create():
             plex = PlexServer(plex_url, plex_token, timeout=30)
             key_str = ",".join(str(k) for k in rating_keys)
             tracks = plex.fetchItems(f"/library/metadata/{key_str}")
             playlist = plex.createPlaylist(title=name, items=tracks)
             return {"success": True, "title": playlist.title, "track_count": len(tracks)}
         return await asyncio.to_thread(_create)
     ```
   - Use batch fetch (comma-separated ratingKeys) to avoid N+1 problem (research Pitfall 6)

3. **Add save_playlist_to_history() to `app/services/chat_service.py`** (per D-17):
   - Creates a Playlist record with name, mood_description (from session messages -- use first user message as description), track_count
   - Creates PlaylistTrack records for each track with position
   - Uses the db_session from the endpoint
   - This provides the data foundation for Phase 5's history view

4. **Create `tests/test_plex_playlist.py`**:
   - test_push_to_plex_success: Mock PlexServer, verify createPlaylist called with correct tracks
   - test_push_to_plex_batch_fetch: Verify fetchItems uses comma-separated keys (not individual fetchItem calls)
   - test_save_playlist_history: Create a playlist via save_playlist_to_history, verify Playlist and PlaylistTrack records exist in DB
   - test_push_requires_playlist_name: Empty name returns 400/error
   - test_push_requires_active_playlist: No current_playlist in session returns error

5. **Update the success flow** (D-16): After successful push, append a success message to the chat like "Playlist '[name]' pushed to Plex with [N] tracks! You can keep refining or start a new playlist." The conversation continues -- session is NOT cleared.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/Composer && python -m pytest tests/test_plex_playlist.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "push-to-plex" app/routers/api_chat.py
    - grep -q "push_playlist_to_plex" app/services/chat_service.py
    - grep -q "save_playlist_to_history" app/services/chat_service.py
    - grep -q "createPlaylist" app/services/chat_service.py
    - grep -q "fetchItems" app/services/chat_service.py
    - grep -q "class Playlist" app/models/playlist.py
    - python -m pytest tests/test_plex_playlist.py -x -q exits 0
  </acceptance_criteria>
  <done>Push-to-Plex endpoint creates named playlist on Plex server using batch fetch (PLAY-06). Playlist saved to history database (D-17). Success message appears in chat, conversation continues (D-16). All tests pass with mocked PlexAPI.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete chat-to-playlist flow</name>
  <files>none</files>
  <read_first>app/templates/pages/chat.html, app/routers/api_chat.py</read_first>
  <action>
Human verification of the complete end-to-end playlist generation flow. No automated changes -- this is a visual and functional review checkpoint.

Steps performed by the user:
1. Start the app and visit http://localhost:8085/
2. Verify chat interface renders correctly at /
3. Test mood preset buttons and chat input
4. Generate a playlist via Ollama (if configured)
5. Test drag-drop reordering and track removal
6. Test push-to-Plex (if Plex available)
7. Verify mobile layout at 375px width
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/Composer && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Full test suite passes
    - User confirms chat UI renders at /
    - User confirms playlist card is interactive
  </acceptance_criteria>
  <what-built>Complete playlist generation pipeline: chat interface at /, mood interpretation via Ollama + Instructor, track scoring and filtering, interactive playlist card with drag-drop reordering, push to Plex. The app's primary user experience.</what-built>
  <how-to-verify>
1. Start the app: `cd /Users/Oreo/Projects/Composer && python -m uvicorn app.main:app --reload --port 8085`
2. Visit http://localhost:8085/ -- should see the chat interface (not old home page)
3. Verify nav bar shows "Compose" as primary link, active state visible
4. Verify mood preset buttons visible: Chill, Energetic, Focus, Late Night, Workout
5. Try clicking a mood preset -- should fill input and submit
6. If Ollama is running and configured: type "chill sunday morning coffee vibes" and submit
7. Wait for response -- should see AI explanation and a playlist card with tracks
8. Verify playlist card shows drag handles and X buttons per track
9. Try dragging a track to reorder (if Ollama available and playlist generated)
10. Try removing a track with the X button
11. Verify track count slider works (change to 10, generate again)
12. If Plex is available: enter a playlist name and click "Push to Plex", verify success message
13. Verify mobile layout: resize browser to ~375px width, confirm single-column layout works
14. Send a refinement message like "add more upbeat tracks" and verify playlist updates
  </how-to-verify>
  <done>User has verified the complete playlist generation flow works end-to-end: chat UI, mood interpretation, playlist display, editing, and push to Plex.</done>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> chat templates | User messages rendered in HTML must be escaped |
| Client -> push-to-plex | Playlist name from user input used in Plex API call |
| LLM response -> chat display | AI explanation text rendered in message bubbles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | chat_message.html | mitigate | Jinja2 auto-escaping on all user and AI content -- never use \|safe filter on untrusted text |
| T-04-08 | Information Disclosure | api_chat.py push-to-plex | mitigate | Sanitize PlexAPI errors before returning to UI -- strip Plex token from exception messages |
| T-04-09 | Tampering | api_chat.py push-to-plex | mitigate | Validate playlist_name is non-empty and reasonable length (<200 chars) before passing to PlexAPI |
| T-04-10 | Denial of Service | playlist_card.html | accept | Alpine.js Sort on large playlists (50 tracks) may be slow on low-end mobile -- acceptable for v1 with max 50 tracks |
</threat_model>

<verification>
- `python -m pytest tests/ -x -q` -- full test suite passes
- Chat page loads at / when Plex is configured
- Mood presets render and disappear after first message
- Playlist card renders with drag handles and remove buttons
- Push to Plex creates playlist (requires running Plex server)
- Mobile layout is single-column at 375px width
</verification>

<success_criteria>
User can visit /, describe a mood, receive an AI-generated playlist, interactively edit it (drag-reorder, remove tracks), adjust track count, and push to Plex as a named playlist. Conversation continues after push. Mobile-first layout works on narrow screens. All templates render without errors. Full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-playlist-generation/04-03-SUMMARY.md`
</output>
