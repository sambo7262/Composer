---
intent: Findings from 08-REVIEW.md that are intentionally deferred to a follow-up phase/task
captured: 2026-05-17 PT
---

# Phase 8 review deferrals → Phase 8.1 polish task

The full code review surfaced 12 findings. Resolved in this phase:

- **CR-01** (Critical) — `/discover` week-cutoff filter on `DiscoveryCandidate.created_at >= WeeklyCronState.last_tick_at`. Fixed in `discovery_service._read_active_discover_data_sync`. Regression test pinned at `tests/test_api_discovery.py::TestDiscoverWeeklyRotation`.
- **WR-01** (Warning) — `run_manual_weekly_tick` no longer overwrites terminal failure state set by inner functions (`artist_discovery_call_weekly`, `discovery_call_weekly`). Also stopped clearing `last_error` on entry.
- **WR-05** (Warning) — Missing `CostMeterBaseline` row now surfaces as "baseline_missing" + pre-first-tick state, instead of silently falling back to epoch and surfacing every historical row in the cost chip.

Deferred to a Phase 8.1 polish task (see `.planning/quick/` once the task is captured):

- **WR-02** — Popularity-gate shared-label arm reads artist-credit names instead of label names. Latent: today `_starred_labels_sync` returns `set()`, so the dead code can't fire. Becomes real when the v1.1 label enrichment lands. Fix is a 5-line rewrite against `mb_artist.get("label-relation-list", [])`.
- **WR-03** — `_lidarr_status_cache` unbounded growth. Acknowledged in Plan 04 SUMMARY as a v1 trade-off. Slow burn: ~1KB per `mb_id`, growth is roughly the size of all artists ever surfaced on /discover. Fix is a bounded LRU or sweep against `DiscoveryAdd WHERE vibe_slotted_at IS NULL`.
- **WR-04** — Lexicographic ISO 8601 comparison on `LLMUsage.called_at` is correct only when microsecond precision is uniform. Today it is (all writes use the same `datetime.now(timezone.utc).isoformat()` shape), but mixing precision would break sort order. Fix is `replace(microsecond=0)` on write OR parse-then-compare on read.

Info findings (six total, all non-functional) — capture during Phase 8.1:

- **IN-01** — Misleading route-order comment in `api_discovery.py`. Reorder literal routes ABOVE catch-all OR update comment to explain segment-count disambiguation.
- **IN-02** — `<noscript>` form fallback on `/debug/discovery` returns raw JSON. Add `HX-Redirect` / `Location` header for non-HTMX form posts.
- **IN-03** — Defensive `id`/`name` key check in `_fetch_lidarr_test_payload` so a Lidarr API shape change doesn't crash with KeyError.
- **IN-04** — Inline rationale comments at the four `except Exception:` sites in `api_discovery.py` (route handlers are Best-effort by Plan 04 D-D5 design).
- **IN-05** — Move `_STALE_WARNING_THRESHOLD_HOURS = 48` to `discovery_service` constants block alongside `DISCOVERY_ARTIST_*`.
- **IN-06** — Regression test pinning `_starred_labels_sync() == set()` to surface coupling with WR-02.

All three Warnings + six Info items together are ~3 hours of polish work — appropriate scope for a Phase 8.1 quick task.
