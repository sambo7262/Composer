---
status: partial
phase: 06.1-vibe-wizard-foundations-server-led-clustering-user-led-vibe-
source: [06.1-VERIFICATION.md]
started: 2026-05-10T23:29:26Z
updated: 2026-05-10T23:29:26Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Real-browser submit of the Step 3 textbox-stack form
expected: POST /api/setup/propose/init body carries the user-typed `vibe_names` JSON array (e.g. `["workout","focus","late night"]`), NOT the initial `[]` placeholder, when the user fills in 3 inputs and clicks "Cluster my library".
why_human: BLOCKER-01 from advisory REVIEW. The form relies on Alpine's `@submit="serialize()"` to copy `names[]` into the hidden `vibe_names` field, but HTMX may serialize the form BEFORE Alpine's submit listener fires.
result: [pending]

### 2. Phase 6.1 migration partial-failure recovery
expected: When `archive_playlist` raises mid-migration (e.g. Plex 401 / timeout), the corresponding `ManagedPlaylist` row should NOT be deleted; the row should remain so the user can retry, AND the Plex playlist's `Composer · {name}` title stays intact so the migration is idempotent on next restart.
why_human: BLOCKER-04 from advisory REVIEW. Current implementation deletes ManagedPlaylist rows unconditionally in `_wipe_sync` after the best-effort archive loop. Only verifiable on a real Plex environment.
result: [pending]

### 3. End-to-end wizard with real LLM + real Plex
expected: Type 3 vibe names → click "Cluster my library" → 3 proposal cards render with fit chips (✓/⚠/✗) AND `member_count` track totals AND 5 closest seed tracks per card. Click "Looks good" → finalize completes synchronously → `/setup/done` page loads only AFTER reslot_all_rated_tracks finishes → every rated track is in at least one `Composer · {name}` Plex playlist with ≥30 tracks per vibe on a 600-track library.
why_human: UAT-level integration; can only be observed against the user's NAS + real Plex + real Anthropic.
result: [pending]

### 4. First-deploy migration on existing Phase 6 state
expected: Upgrade a container that has existing Phase 6 vibes + Plex `Composer · {name}` playlists. After restart, Plex playlists are renamed to `Composer · {name} (archived)`, all Vibe/TrackVibe/ManagedPlaylist/SlotInLog rows are gone, SetupState resets to step='rating_source', `GET /` redirects to `/setup`. Second restart is a no-op.
why_human: Cannot reproduce real Plex playlist archival in tests; mocks verify the call shape (4-arg signature) but not the actual Plex side-effect.
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps
