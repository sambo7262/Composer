---
phase: 06.1-vibe-wizard-foundations-server-led-clustering-user-led-vibe-
verified: 2026-05-10T23:27:08Z
updated: 2026-05-29
status: verified
score: 18/18 must-haves verified — human-verification items confirmed via NAS UAT 2026-05-29 (see 06.1-HUMAN-UAT.md)
overrides_applied: 0
human_verification:
  - test: "Real-browser submit of the Step 3 textbox-stack form"
    expected: "POST /api/setup/propose/init body carries the user-typed `vibe_names` JSON array (e.g. `[\"workout\",\"focus\",\"late night\"]`), NOT the initial `[]` placeholder, when the user fills in 3 inputs and clicks 'Cluster my library'."
    why_human: "BLOCKER-01 from advisory REVIEW. The form relies on Alpine's `@submit=\"serialize()\"` to copy `names[]` into the hidden `vibe_names` field, but HTMX may serialize the form BEFORE Alpine's submit listener fires. Unit tests verify the hidden input exists and integration tests POST form-data directly (bypassing the template), so this timing question can only be resolved in a real browser/HTMX runtime."
    surfaced_by: "06.1-REVIEW.md BLOCKER-01"
  - test: "Phase 6.1 migration partial-failure recovery"
    expected: "When `archive_playlist` raises mid-migration (e.g. Plex 401 / timeout), the corresponding `ManagedPlaylist` row should NOT be deleted; the row should remain so the user can retry, AND the Plex playlist's `Composer · {name}` title stays intact so the migration is idempotent on next restart."
    why_human: "BLOCKER-04 from advisory REVIEW. Current implementation deletes ManagedPlaylist rows unconditionally in `_wipe_sync` after the best-effort archive loop. Only verifiable on a real Plex environment that produces a real error during archive. The tight except clause also excludes `asyncio.TimeoutError`, `ConnectionError`, `OSError`, which would abort the migration loop before the gate marker is set."
    surfaced_by: "06.1-REVIEW.md BLOCKER-04 + WARNING-09"
  - test: "End-to-end wizard with real LLM + real Plex"
    expected: "Type 3 vibe names → click 'Cluster my library' → 3 proposal cards render with fit chips (✓/⚠/✗) AND `member_count` track totals AND 5 closest seed tracks per card. Click 'Looks good' → finalize completes synchronously → `/setup/done` page loads only AFTER reslot_all_rated_tracks finishes → every rated track is in at least one `Composer · {name}` Plex playlist with ≥30 tracks per vibe on a 600-track library."
    why_human: "UAT-level integration; can only be observed against the user's NAS + real Plex + real Anthropic. The unit tests + the synchronous-await mock test prove the wiring; the end-to-end behavior on the user's library is the success criterion."
  - test: "First-deploy migration on existing Phase 6 state"
    expected: "Upgrade a container that has existing Phase 6 vibes + Plex `Composer · {name}` playlists. After restart, Plex playlists are renamed to `Composer · {name} (archived)`, all Vibe/TrackVibe/ManagedPlaylist/SlotInLog rows are gone, SetupState resets to step='rating_source', `GET /` redirects to `/setup`. Second restart is a no-op."
    why_human: "Cannot reproduce real Plex playlist archival in tests; mocks verify the call shape (4-arg signature) but not the actual Plex side-effect."
---

# Phase 06.1: Vibe Wizard Foundations Verification Report

**Phase Goal:** Replace the broken Phase 6 initial-proposal flow with server-led k-means membership (every rated track lands in its nearest vibe) and user-led Step 3 input (user types 3–7 vibe names; ONE LLM call maps cluster centroids to names with a strong/weak/no_match fit grade); ship a one-shot first-deploy migration that archives existing Composer · Plex playlists and wipes the broken state.

**Verified:** 2026-05-10T23:27:08Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Server-led k-means labels populate `seed_track_indices` for every cluster — the LLM never picks members | VERIFIED | `app/services/vibe_clusterer.py:1209` `seed_track_indices=list(sorted_idxs)` populated server-side from k-means labels. Regression test `test_map_user_vibes_server_populates_seed_track_indices_seed_tracks_members` asserts `sum(len(p.seed_track_indices)) == 90` (full partition). |
| 2 | Every proposal carries `seed_tracks` (5 closest-to-centroid, list of `{title, artist, rating_key}`) AND `members` (full list) so the card template renders real song data | VERIFIED | `vibe_clusterer.py:1210-1211` populate `seed_tracks=seed_tracks` (top 5) and `members=members` (all). Tests `test_map_user_vibes_server_populates_seed_track_indices_seed_tracks_members` + `test_vibe_members_disclosure_renders_all_members_when_expanded` verify dict shape with non-empty title/artist/rating_key. |
| 3 | Every proposal carries `member_count` (= len(members)) for the fit-chip display count | VERIFIED | `vibe_clusterer.py:1212` `member_count=len(members)`. Template at `vibe_proposal_card.html:41` reads `proposal.member_count`. Test `test_vibe_proposal_card_uses_member_count_for_fit_chip_display` asserts the chip shows `87 tracks` from `member_count=87` even when `seed_track_indices` length differs (3). |
| 4 | Per-cluster `top_genres` (top 3 by Track.genre frequency) plumbed into the LLM mapping prompt for genre-driven names | VERIFIED | `vibe_clusterer.py:308` `"top_genres": top_genres` in cluster_summaries. Prompt builder `_build_user_led_clustering_user_prompt:571` mentions "`top_genres` list per cluster is your primary signal". Test `test_user_led_user_prompt_includes_positional_ids_top_genres_and_explicit_instruction` asserts "top_genres" + "synth-pop" + "synthwave" appear in prompt. |
| 5 | User-typed vibe names map 1:1 to k-means clusters via ONE LLM call (`purpose=vibe_clustering_user_led`); casing preserved via casefold lookup table | VERIFIED | `vibe_clusterer.py:1152` `purpose="vibe_clustering_user_led"`. Casefold lookup at lines 1017-1020 with original-cased values. Test `test_map_user_vibes_preserves_user_name_casing_on_llm_lowercase_echo` verifies user's `["Workout", "Late Night Drive", "Synth Heavy"]` survives the LLM's lowercase echo. |
| 6 | Every proposal carries a `fit` grade (strong/weak/no_match) from the LLM | VERIFIED | `VibeProposal.fit: Optional[Literal["strong","weak","no_match"]]` at `vibe_clusterer.py:117`. `LLMVibeFit.fit` required at line 190. `map_user_vibes_to_clusters` constructs `VibeProposal(..., fit=m.fit)` at line 1213. Tests `test_vibe_proposal_card_renders_fit_chip_strong/weak/no_match` cover all 3 states. |
| 7 | `no_match` proposals carry a non-empty `fit_reason` from the LLM | VERIFIED | `_validate_mapping_permutation:587-591` rejects `fit=="no_match"` with empty `reason`. Test `test_validate_mapping_permutation_detects_missing_reason_on_no_match` asserts validator rejects it with retry. |
| 8 | Permutation validation on LLM response with retry-once on failure | VERIFIED | `_validate_mapping_permutation` (vibe_clusterer.py:578) + retry logic at lines 1158-1180. Test `test_map_user_vibes_retries_once_on_permutation_failure_then_raises` asserts exactly 2 LLM calls then `ValueError("failed permutation validation")`. |
| 9 | Fit + fit_reason survive the refine-loop round-trip via case-insensitive name match (Blocker #5 Option A) | VERIFIED | `_carryover_fit_from_prior` at `vibe_clusterer.py:616` + wiring inside `_call_llm_with_validation`. Test `test_carryover_fit_from_prior_matches_case_insensitive_name` covers case-mismatch, no-match, idempotency. |
| 10 | sklearn import remains confined to vibe_clusterer.py (Phase 6 D-33 AST tripwire stays green) | VERIFIED | Test `tests/test_vibe_clusterer.py::test_sklearn_only_in_clusterer_module` passes (re-confirmed). |
| 11 | asyncio.to_thread invariant holds for any DB/PlexAPI access from async (Phase 5 D-09 AST tripwire) | VERIFIED | Test `tests/test_event_handlers.py::TestStaticAnalysis` passes (re-confirmed). All new sync DB reads inside `run_phase_61_migration` dispatched through `asyncio.to_thread`. |
| 12 | Step 3 opens with textbox-stack input (3 default, +Add up to 7) — NOT an LLM-led proposal call | VERIFIED | `app/templates/pages/setup_step3.html:25-92` renders Alpine x-data form with `names: ['', '', '']` default; no `hx-trigger="load"` (grep == 0). Test `test_setup_step3_initial_render_shows_textbox_stack` asserts `name="vibe_names"` + `+ Add another` + `Cluster my library` + no `hx-trigger="load"`. |
| 13 | Form submission validates count (3-7), trims blanks, rejects case-insensitive duplicates, calls map_user_vibes_to_clusters | VERIFIED | `app/routers/api_setup.py:218-326` propose_init parses JSON → trims → casefold-dedupes → 3-7 count check → calls `map_user_vibes_to_clusters`. Tests `test_propose_init_rejects_count_below_3 / _above_7 / _rejects_case_insensitive_duplicates / _trims_blanks_before_count_check / _happy_path_calls_map_user_vibes` all pass. |
| 14 | Every vibe_proposal_card renders a fit-grade chip (✓ strong / ⚠ weak / ✗ no_match) using proposal.member_count for the count display | VERIFIED | `vibe_proposal_card.html:22-51` conditional block on `proposal.fit` with text-success/accent/error tokens + Alpine "Why?" toggle for fit_reason. `member_count` used on line 41. 5 fit-chip rendering tests pass (strong, weak, no_match w/reason, no chip when None, member_count source-of-truth). |
| 15 | Finalize is SYNCHRONOUS: awaits reslot_all_rated_tracks before returning HTTP response (Blocker #7 Option A) | VERIFIED | `api_setup.py:639` `slotted = await reslot_all_rated_tracks()` inside success branch. `grep "asyncio.create_task(reslot" api_setup.py` == 0 (no fire-and-forget). Test `test_finalize_synchronously_awaits_reslot_all_rated_tracks` asserts `reslot_mock.await_count == 1` by the time HTTP response returns. Companion test `test_finalize_does_not_call_reslot_on_partial_failure` asserts reslot is NOT called when create_playlist fails. |
| 16 | First-deploy migration archives existing 'Composer · ' Plex playlists via archive_playlist with FULL 4-arg signature; calls succeed without TypeError | VERIFIED | `app/main.py:145-148` calls `_pps.archive_playlist(plex_url, plex_token, mp.plex_rating_key, mp.composer_name)` (4 positional args). Test `test_run_phase_61_migration_archives_legacy_playlists` asserts `len(call.args) == 4` AND `first_args == ("http://localhost:32400", "fake-token", "rk-1", "Composer · OldVibe")`. |
| 17 | Migration's except clause is TIGHT: catches (PlexApiException, httpx.HTTPError, PermissionError, TypeError) — NOT bare Exception (WARNING #9) | VERIFIED | `app/main.py:152-157` explicit tuple. `awk '/async def run_phase_61_migration/,/^async def [a-z]\|^def [a-z]/' app/main.py | grep -c "except Exception"` == 0. Test `test_run_phase_61_migration_surfaces_arity_typeerror` confirms TypeError surfaces in logs. |
| 18 | Migration clears Vibe/TrackVibe/ManagedPlaylist/SlotInLog/SetupState exactly once per container; running twice has no further side effects | VERIFIED | `app/main.py:170-217` `_wipe_sync` deletes in FK-safe order + resets SetupState + clears Track.pending_slot_in + marks `MigrationLog.completed_at`. Gate check at lines 91-95. Tests `test_run_phase_61_migration_records_completion_on_empty_db / _clears_existing_vibe_state / _clears_pending_slot_in / _idempotent_no_double_archive` all pass. |

**Score:** 18/18 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/services/vibe_clusterer.py` | LLMVibeFit + LLMVibeMappingResponse schemas; map_user_vibes_to_clusters() with server-populated fields; _validate_mapping_permutation() + retry-once; VibeProposal field extensions; refine-loop fit/fit_reason carryover | VERIFIED | Lines 179, 194, 974, 578, 105-119, 616. All 6 symbols present; grep verified. |
| `tests/test_vibe_clusterer.py` | Regression coverage for server-populated fields, fit grade round-trip + refine survival, top_genres in prompt, name-casing, permutation + retry | VERIFIED | 35 tests, all pass. |
| `app/routers/api_setup.py` | propose_init accepts vibe_names Form field; calls map_user_vibes_to_clusters; finalize AWAITS reslot_all_rated_tracks | VERIFIED | Lines 222, 307, 639. |
| `app/templates/pages/setup_step3.html` | Textbox-stack input on initial render; proposal cards on post-mapping render | VERIFIED | Lines 25-92 (form) + 17-20 (cards branch). |
| `app/templates/partials/vibe_proposal_card.html` | Fit-grade chip rendering using proposal.member_count | VERIFIED | Lines 16-51 + line 41 (`member_count`). |
| `app/main.py` | Lifespan startup hook: run_phase_61_migration() called once after init_db; archive_playlist called with 4 args | VERIFIED | Line 33 (def), line 244 (lifespan call), lines 145-148 (4-arg archive). |
| `app/models/vibe.py` | MigrationLog SQLModel table | VERIFIED | Line 195. |
| `app/database.py` | MigrationLog registered in init_db | VERIFIED | Lines 183-187. |
| `tests/test_phase61_migration.py` | Migration idempotency tests; uses autouse tmp_data_dir fixture | VERIFIED | 6 tests pass; `grep "monkeypatch.setenv.*COMPOSER_DB_PATH"` == 0 (Blocker #4 honored). |
| `app/templates/partials/vibe_members_disclosure.html` | Reads proposal.members for "Show all N tracks" | VERIFIED | Line 12 `for member in (proposal.members | default([]))`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `map_user_vibes_to_clusters` | `_pick_best_k` with forced_k=N | k-means labels drive seed_track_indices + members grouping | WIRED | `vibe_clusterer.py:1037-1039` `_pick_best_k(X_normalized, n_rated, forced_k=n_targets)`. |
| `map_user_vibes_to_clusters` | `LLMVibeMappingResponse` | AnthropicClient call_with_structured_output with purpose=vibe_clustering_user_led | WIRED | Lines 1146-1153. |
| `_validate_mapping_permutation` | retry-once on failure | corrective user prompt | WIRED | Lines 1158-1180; test asserts exactly 2 calls. |
| `LLMVibeFit` | `VibeProposal.fit + VibeProposal.fit_reason` | server-side resolution + carryover | WIRED | Lines 1213-1214 (direct), 616 (_carryover_fit_from_prior). |
| `VibeProposal.seed_tracks + members` | vibe_proposal_card.html + vibe_members_disclosure.html | JSON-serializable dicts surviving SetupState.draft_proposals_json round-trip | WIRED | Round-trip test `test_vibe_proposal_accepts_seed_tracks_members_member_count_fields` passes. |
| `api_setup.propose_init` | `map_user_vibes_to_clusters` | lazy-import shim + Form-parsed vibe_names list | WIRED | Lines 66-72 (shim) + 307 (call). |
| `setup_step3.html` | `/api/setup/propose/init` | form hx-post with vibe_names input stack | WIRED | Line 26 `hx-post="/api/setup/propose/init"`. **NOTE:** BLOCKER-01 in advisory review flags timing concern between Alpine `@submit` and HTMX form serialization — not provable in static tests, flagged for human verification. |
| `vibe_proposal_card.html` | `proposal.fit + fit_reason + member_count` | Jinja conditional render with ✓/⚠/✗ icons + Alpine x-show tooltip | WIRED | Lines 22-51; 7 template tests pass. |
| `api_setup.finalize` | `reslot_all_rated_tracks` | AWAITED call after Vibe + ManagedPlaylist rows committed | WIRED | Line 639. Test `test_finalize_synchronously_awaits_reslot_all_rated_tracks` confirms `await_count==1` when response returns. |
| `app/main.run_phase_61_migration` | `plex_playlist_service.archive_playlist` | 4-arg call; FK-safe order; tight except clause | WIRED | Lines 145-157. Test asserts 4-arg call signature. |
| `app/main.lifespan` | `run_phase_61_migration` | called once after init_db, gated by MigrationLog row | WIRED | Line 244. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `vibe_proposal_card.html` | `proposal.seed_tracks` + `proposal.members` + `proposal.member_count` + `proposal.fit` + `proposal.fit_reason` | `map_user_vibes_to_clusters` returns VibeProposalSet with these populated from k-means labels + LLM mapping → persisted via `state.draft_proposals_json` (SetupState row) → re-hydrated on Step 3 render | FLOWING | k-means labels partition the rated set (test verifies sum==90 on 90-track fixture). LLM mapping populates fit grade. Round-trip persistence verified by `test_vibe_proposal_accepts_seed_tracks_members_member_count_fields`. |
| `setup_step3.html` (form-mode) | `names: []` (Alpine x-data) | User input via textbox-stack | FLOWING (with caveat) | Alpine `names` array driven by `x-model`. **CAVEAT:** Order-of-operations between Alpine `@submit` and HTMX request serialization is the BLOCKER-01 concern — flagged for human verification. |
| `api_setup.finalize` | rated tracks → vibes via `reslot_all_rated_tracks` → `slot_track` per ratingKey | `_read_all_rated_track_keys_sync` (DB) → `slot_track` (per-track lock, soft margin, additive Plex push) | FLOWING | `slot_track` is the existing Phase 6 hot path; reslot iterates real rated keys with semaphore=1; observed by test that mocks reslot to capture await_count. |
| `app/main.run_phase_61_migration` | `managed = ManagedPlaylist` rows + `(plex_url, plex_token)` settings | DB reads via `asyncio.to_thread(_read_managed_playlists_sync)` + `_get_plex_creds_sync` | FLOWING | Tests `test_run_phase_61_migration_clears_existing_vibe_state` + `_archives_legacy_playlists` seed real DB rows and verify they get consumed. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Plan 01: full clusterer test suite (35 tests) | `.venv/bin/python -m pytest tests/test_vibe_clusterer.py -q` | 35 passed | PASS |
| Plan 02: migration test suite (6 + 12 schema) | `.venv/bin/python -m pytest tests/test_phase61_migration.py tests/test_database_phase6.py -q` | 18 passed | PASS |
| Plan 02: wizard / template / finalize test suite | `.venv/bin/python -m pytest tests/test_api_setup.py tests/test_setup_templates.py tests/test_finalize_integration.py tests/test_recluster_commit.py tests/test_event_handlers.py -q` | 68 passed | PASS |
| Phase 06.1 full suite | `.venv/bin/python -m pytest tests/test_vibe_clusterer.py tests/test_phase61_migration.py tests/test_database_phase6.py tests/test_api_setup.py tests/test_setup_templates.py tests/test_finalize_integration.py tests/test_recluster_commit.py tests/test_event_handlers.py -q` | 121 passed | PASS |
| Phase 5 D-09 + Phase 6 D-33 AST tripwires | `.venv/bin/python -m pytest tests/test_vibe_clusterer.py::test_sklearn_only_in_clusterer_module tests/test_event_handlers.py::TestStaticAnalysis -q` | 2 passed | PASS |
| Grep guard: no fire-and-forget reslot | `grep -c "asyncio.create_task(reslot_all_rated_tracks" app/routers/api_setup.py` | 0 | PASS |
| Grep guard: no bare except in migration | `awk '/async def run_phase_61_migration/,/^async def \[a-z\]\|^def \[a-z\]/' app/main.py \| grep -c "except Exception"` | 0 | PASS |
| Grep guard: no text-[15px] on new templates | `grep -c "text-\[15px\]" app/templates/pages/setup_step3.html app/templates/partials/vibe_proposal_card.html` | 0 + 0 | PASS |
| Grep guard: no `hx-trigger="load"` on Step 3 initial render | `grep -c "hx-trigger=\"load\"" app/templates/pages/setup_step3.html` | 0 | PASS |
| Grep guard: no wrong env var in migration tests | `grep -c "monkeypatch.setenv.*COMPOSER_DB_PATH" tests/test_phase61_migration.py` | 0 | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| VIBE-02 | 06.1-01, 06.1-02 | TrackVibe link table with soft-membership cap=2 | SATISFIED | Existing TrackVibe model (Phase 6) + Phase 6.1's synchronous reslot via `slot_track` exercises soft-margin path on every rated track post-finalize. Test `test_finalize_synchronously_awaits_reslot_all_rated_tracks` confirms slot pipeline runs. |
| VIBE-04 | 06.1-02 | Composer · {name} namespace prefix; never read/modify user-created playlists | SATISFIED | Phase 6.1 migration archives ONLY `ManagedPlaylist` rows (which by Phase 6 convention only contain `Composer · ` playlists). `archive_playlist` (4-arg) preserves the prefix and appends ` (archived)`. Test `test_run_phase_61_migration_archives_legacy_playlists` verifies. |
| VIBE-05 | 06.1-01 | k-means on Essentia 4-D normalized with k between 3-7 + silhouette ≥ 0.25 | SATISFIED | `map_user_vibes_to_clusters` calls `_pick_best_k(X_normalized, n_rated, forced_k=n_targets)` with `n_targets` constrained to 3-7 via input validation (`vibe count must be 3-7`). Silhouette threshold + degraded_mode flow preserved from Phase 6. |
| VIBE-09 | 06.1-01, 06.1-02 | On RatingChanged, track scored + slotted + Plex playlist updated additively | SATISFIED | Phase 6's slot_track hot path is preserved (no changes to vibe_service.slot_track semantics — confirmed by `git diff`). Phase 6.1's synchronous reslot on finalize uses the same slot_track for initial population, validating the path end-to-end. |
| WIZ-05 | 06.1-01, 06.1-02 | Step 3: Propose vibes — runs clustering, displays each proposed cluster with seed tracks + auto-generated name and description; user edits inline | SATISFIED | Phase 6.1 inverts who picks: user types names, server clusters, LLM names+descriptions. Step 3 now shows real seed tracks (top 5) + name+description editable inline via `vibe_name_input.html` (existing Phase 6 partial). Fit chip is an enhancement. 8 propose_init tests + 7 template tests confirm. |

All 5 declared requirement IDs (VIBE-02, VIBE-04, VIBE-05, VIBE-09, WIZ-05) are SATISFIED. No orphaned requirements — REQUIREMENTS.md does not map additional IDs to Phase 6.1 (the inserted decimal phase fixes Phase 6's broken implementation under the same IDs).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `app/templates/pages/setup_step3.html` | 46 | `@submit="serialize()"` — Alpine listener may fire AFTER HTMX serializes form | Warning | BLOCKER-01 in advisory review. Cannot verify in unit tests. Flagged for human browser testing. |
| `app/main.py` | 152-157 | Tight except clause excludes `asyncio.TimeoutError`, `ConnectionError`, `OSError` | Info | WARNING-09 in advisory review. Plex outages during first-deploy could abort migration mid-loop before gate marker is set. |
| `app/main.py` | 170-217 (`_wipe_sync`) | Deletes ALL ManagedPlaylist rows unconditionally after best-effort archive | Warning | BLOCKER-04 in advisory review. If archive_playlist failed for any row, the DB row is still deleted, leaving orphan Plex playlist with no audit trail. Edge case (Plex must be reachable for normal first-deploy). Flagged for human verification. |
| `app/services/vibe_clusterer.py` | 733-742 (`materialize_clusters`) | `merged_from`/`split_from` actions ignored — uses only `seed_track_indices` | Info | BLOCKER-03 in advisory review. Out of scope for Phase 6.1's user-led initial-proposal path; affects refinement-loop accuracy (Phase 6 territory). Not a regression caused by 6.1. |
| `app/database.py` | 199-202 | `_migrate_add_columns` bare `except Exception: pass` | Info | BLOCKER-02 in advisory review. Pre-existing — NOT introduced by Phase 6.1. Out of scope. |

### Human Verification Required

Four items require real-environment validation that programmatic tests cannot cover:

#### 1. Real-browser submit of Step 3 textbox-stack form

**Test:** Open `/setup/propose` in a real browser (HTMX 2.x + Alpine 3.x as configured), type 3 vibe names ("workout", "focus", "late night"), click "Cluster my library", inspect the network request body.
**Expected:** POST body contains `vibe_names=["workout","focus","late night"]` (the user-typed values), NOT `vibe_names=[]` (the initial hidden-field value).
**Why human:** BLOCKER-01 from advisory REVIEW. The form uses `@submit="serialize()"` Alpine handler to copy `names[]` into the hidden field, but HTMX's request serialization order vs. Alpine's submit event listener is runtime-dependent and not verifiable in Jinja unit tests (which only check the hidden input exists) or integration tests (which POST form-data directly, bypassing the template).

#### 2. Phase 6.1 migration partial-failure recovery

**Test:** With existing Phase 6 ManagedPlaylist rows + Plex temporarily unreachable, restart container and observe migration behavior.
**Expected:** ManagedPlaylist rows for which `archive_playlist` raised should remain in the DB (so user can retry); MigrationLog gate should still mark phase complete OR cleanly bail without marking complete so the next restart retries.
**Why human:** BLOCKER-04 from advisory REVIEW. Current `_wipe_sync` deletes all ManagedPlaylist rows after the best-effort archive loop, regardless of which archives failed. Cannot reproduce real Plex failures in unit tests.

#### 3. End-to-end wizard with real LLM + real Plex

**Test:** Complete the wizard on the user's NAS with a 600-track library and real Anthropic credentials.
**Expected:** 3-7 proposal cards render with real fit chips + real `member_count` + real seed tracks. Finalize completes; `/setup/done` loads only after slotting finishes. Each `Composer · {name}` Plex playlist contains ≥30 tracks (~85 expected on a 600-track 7-vibe library).
**Why human:** Cannot mock real Plex playlist creation + real Anthropic LLM mapping. The unit tests prove wiring; this proves outcome.

#### 4. First-deploy migration on existing Phase 6 state

**Test:** Upgrade a container that has populated Vibe/TrackVibe/ManagedPlaylist + real `Composer · {name}` Plex playlists. Observe first restart.
**Expected:** Plex playlists renamed to `Composer · {name} (archived)`; DB tables wiped (Vibe.count==0, TrackVibe.count==0, ManagedPlaylist.count==0, SlotInLog.count==0); SetupState resets to step='rating_source'; `GET /` redirects to `/setup`. Second restart is a no-op (gate row prevents re-run).
**Why human:** Cannot validate real Plex archival side-effects in tests; mocks only confirm 4-arg call signature.

### Gaps Summary

No code-level gaps blocking the phase goal. All 18 must-have observable truths are VERIFIED by codebase evidence + 121 passing tests + 0 anti-pattern guards (no fire-and-forget, no bare except in migration, no wrong env var, no text-[15px], no hx-trigger=load on initial render). 5/5 declared requirements (VIBE-02, VIBE-04, VIBE-05, VIBE-09, WIZ-05) SATISFIED.

Four items require human verification (browser + real Plex + real Anthropic) — listed above. The advisory REVIEW.md surfaces 4 BLOCKER findings; two are direct code-review concerns about Phase 6.1 (BLOCKER-01 Alpine/HTMX submit timing, BLOCKER-04 ManagedPlaylist orphan risk) and two are out-of-scope or pre-existing (BLOCKER-02 `_migrate_add_columns` pre-existing; BLOCKER-03 `materialize_clusters` Phase 6 territory).

The phase goal is achieved at the codebase level. The two in-scope advisory blockers (BLOCKER-01 + BLOCKER-04) are real concerns but require either (a) real-browser verification, or (b) Plex-failure injection, to confirm whether they cause user-visible failures in practice.

---

_Verified: 2026-05-10T23:27:08Z_
_Verifier: Claude (gsd-verifier)_
