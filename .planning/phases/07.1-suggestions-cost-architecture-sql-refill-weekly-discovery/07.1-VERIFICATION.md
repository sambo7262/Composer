---
phase: 07.1-suggestions-cost-architecture-sql-refill-weekly-discovery
verified: 2026-05-15T22:59:43Z
updated: 2026-05-16T07:30:00Z
status: gaps_found
score: 27/27 must-haves verified (live UAT surfaced 2 gaps)
overrides_applied: 0
gaps:
  - id: GAP-01
    title: "Discovery LLM call prompt exceeds Claude 200K input context on cold-start catch-up"
    severity: blocking
    in_scope: 07.1
    discovered: 2026-05-16 NAS UAT
    evidence: |
      NAS log 2026-05-16 07:15:44 — catch-up gate fired at +10s after startup as designed (Plan 02 D-C2),
      but discovery_call_weekly raised BadRequestError: "prompt is too long: 202027 tokens > 200000 maximum"
      from app/services/suggestions_discovery.py:695 (call_with_structured_output).
      Error handler correctly logged and skipped retry (only MaxTokensTruncationError retries — Blocker #5).
      Net effect: catch-up scheduling works, but no LLMUsage row written, no SuggestionsMirror picks added,
      DiscoveryState.last_discovery_run_at NOT stamped. UAT item #2 fails on the live LLM call.
    root_cause: |
      Phase 7.1 capped the OUTPUT side (DISCOVERY_MAX_TOKENS_FLOOR=8000, MaxTokensTruncationError retry),
      but the INPUT-side candidate pool from compute_discovery_eligible has no token budget. With a ~10.5K
      track library and cold-start state (no SuggestionHistory, no plays in 90+ days for many tracks),
      the candidate list is large enough that prompt assembly blows past 200K when track metadata is included.
    fix_direction: |
      Cap candidate pool before LLM call. Two complementary controls: (1) hard LIMIT in
      _read_discovery_eligible_sync (e.g., ORDER BY last_viewed_at NULLS FIRST + LIMIT 200), and
      (2) per-prompt token budget in discovery_call_weekly that trims candidates if rough token estimate
      exceeds a safe ceiling (e.g., 150K input). Keep existing max_tokens floor + MaxTokensTruncationError retry.
      Add regression test: call discovery_call_weekly against a 10K-track fixture and assert prompt <= 150K tokens.
  - id: GAP-02
    title: "Frontend library_stats partial polls /api/library/stats 25-50 times/second"
    severity: warning
    in_scope: pre-existing (predates 07.1) — bundled into 07.1 gap closure per user direction
    discovered: 2026-05-16 NAS UAT
    evidence: |
      NAS log 2026-05-16 07:15:53 - 07:17:05 — /api/library/stats hit 25-50x per second across multiple
      client ports (Tailscale proxy multiplexing 2-3 open tabs on /settings + /debug pages). Bandwidth
      and DB load problem for a Synology Celeron J4125 NAS.
    root_cause: |
      app/templates/partials/library_stats.html line 3 declares `hx-trigger="load, every 10s"` combined
      with line 5 `hx-swap="outerHTML"`. Each swap replaces the element with a new copy that ALSO carries
      `load` — htmx fires `load` immediately on the new element, triggering another swap, infinite loop.
      The `every 10s` portion is effectively dead. Bug predates Phase 7.1 (template lives back through 06).
      Settings page (settings.html:9), debug_vibes (line 8), and debug_events (line 5) all embed the partial.
    fix_direction: |
      Remove `load,` from hx-trigger so the partial only polls on the `every 10s` cadence (the initial
      render happens server-side when the parent page mounts the partial). Add a smoke test that
      mounts the partial under htmx and asserts only one /api/library/stats call within a 1s window.
human_verification:
  - test: "Settings page cost meter renders weekly framing on the NAS"
    expected: "GET /settings shows 'Anthropic spend this week' heading, $X / $0.50 budgeted, and weekly cadence cold-start copy (no 'Anthropic spend today' or 'caching not engaged (system prompt may be below 2048 tokens)' strings)"
    why_human: "Visual / template render verification requires running the FastAPI app + a real LLMUsage row population over the rolling 7-day window; AST + import tests cover the source side but not the rendered HTML in a real browser"
  - test: "First-deploy startup catch-up fires within 10 seconds on the NAS"
    expected: "On first restart after 7.1 deploy, logs show 'Last discovery run is NULL or > 7 days old — scheduling catch-up discovery_call_weekly in 10s' followed (10 seconds later) by an Anthropic call with purpose='discovery_weekly' and an LLMUsage row written. SuggestionsMirror gains 3-7 new rows with discovery_weekly event_source. DiscoveryState.last_discovery_run_at is stamped."
    why_human: "End-to-end live verification of the D-C2 catch-up gate requires real Anthropic credentials + real Plex library + the APScheduler running in production lifespan; unit tests stub the LLM client and the timing"
  - test: "Sunday 03:00 UTC cron actually fires on the NAS"
    expected: "After 7+ days, on the next Sunday 03:00 UTC, a discovery_call_weekly LLMUsage row appears and SuggestionsMirror gains discovery picks"
    why_human: "Time-based cron verification requires waiting for the actual schedule; covered statically by AST tests + register-time assertions but not behaviorally"
  - test: "Every TrackPlay webhook event refills the mirror via SQL with $0 LLM cost"
    expected: "Play 8+ tracks in a row on Plex; observe (a) LLMUsage row count does NOT increase (no per-play LLM calls), (b) SuggestionsMirror.count() stays at ~30 (drained-and-refilled), (c) cost-meter spend stays flat over the play burst"
    why_human: "Real Plex webhook delivery + real listening session needed; unit tests cover handler logic with stubbed mirror but not the live webhook → handler → SQL refill chain on the NAS"
---

# Phase 7.1: Suggestions Cost Architecture Verification Report

**Phase Goal:** Land Phase 7.1's "Option C" architecture — replace the LLM-on-every-refill suggestions hot path with SQL-driven refill from pre-computed TrackVibe.distance (zero LLM tokens per play), and introduce a weekly LLM discovery layer that backfills new candidates when plays_since_last_discovery crosses the eligibility threshold. Add cost-meter relabel + AST regression guards to lock the architecture in place.

**Verified:** 2026-05-15T22:59:43Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

All must_haves merged from ROADMAP success_criteria (6) + Plan 01 (10) + Plan 02 (8) + Plan 03 (11) — deduplicated to the load-bearing observable truths below.

| #   | Truth                                                                                                                                                                      | Status     | Evidence                                                                                                                                                                                                                                            |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | (SC#1, Plan 01) Refill on every play uses ZERO LLM tokens — SQL query against `TrackVibe.distance` ordered ASC                                                             | ✓ VERIFIED | `app/services/suggestions_service.py:721` `async def refill_mirror_sql`; `_read_top_n_for_vibe_sync` uses `text("... ORDER BY tv.distance ASC LIMIT :n")` (lines 580-617); no LLM client call in module (AST guard `test_no_anthropic_call_in_suggestions_service` passes) |
| 2   | (Plan 01) `refill_suggestions_queue`, `refill_suggestions_for_vibe`, and `SUGGESTIONS_RANK_MAX_TOKENS` are removed entirely from suggestions_service.py                    | ✓ VERIFIED | `grep -n 'SUGGESTIONS_RANK_MAX_TOKENS\|refill_suggestions_queue\|refill_suggestions_for_vibe' app/services/suggestions_service.py` → 0 matches. Import test confirms `ImportError`. AST guards `TestNoLegacyLlmRefillRefs` (4 tests) all PASS live |
| 3   | (Plan 01) `DiscoveryState` single-row id=1 SQLModel table holds plays_since_last_discovery + last_discovery_run_at                                                          | ✓ VERIFIED | `app/models/vibe.py:208` `class DiscoveryState(SQLModel, table=True)` with `id: Optional[int] = Field(default=1, primary_key=True)`; registered in `app/database.py:212`                                                                              |
| 4   | (Plan 01) `LLMUsage.error_text` Optional[str] column added via `_migrate_add_columns` ALTER                                                                                | ✓ VERIFIED | `app/models/llm_usage.py:33` `error_text: Optional[str] = Field(default=None)`; migration at `app/database.py:152-164` with PRAGMA-guarded `ALTER TABLE llmusage ADD COLUMN error_text TEXT`                                                       |
| 5   | (Plan 01) `MaxTokensTruncationError(Exception)` raised — NOT warned — on `response.stop_reason == "max_tokens"`                                                            | ✓ VERIFIED | `app/services/anthropic_client.py:59` class defined; line 221 `if stop_reason == "max_tokens":` → line 250 `raise MaxTokensTruncationError(purpose=..., requested_max_tokens=..., truncated_text_length=...)`                                       |
| 6   | (Plan 01) `handle_track_played` increments `plays_since_last_discovery` (best-effort try/except + lazy import)                                                             | ✓ VERIFIED | `app/services/event_handlers.py:281-289` — `try: from app.services import suggestions_discovery; await suggestions_discovery.increment_plays_since_last_discovery() except Exception: logger.exception(...)`                                          |
| 7   | (Plan 01) Both `_read_vibe_pool_weights_sync` AND `_read_top_n_for_vibe_sync` filter `Vibe.is_active = 1` (archived vibes excluded — Blocker #7)                            | ✓ VERIFIED | `app/services/suggestions_service.py:552` (`WHERE v.is_active = 1` in pool weights) and `:590` (`AND v.is_active = 1` in top-N JOIN). Three regression tests confirm end-to-end                                                                       |
| 8   | (Plan 01) Phase 5 D-09 AST invariant — every `Session(get_engine())` in `suggestions_discovery.py` lives in a `_*_sync` helper                                              | ✓ VERIFIED | `tests/test_suggestions_discovery.py:1232` `test_no_session_outside_sync_helper_in_suggestions_discovery` present; also `test_all_sql_helpers_called_via_to_thread` PASSES live                                                                       |
| 9   | (Plan 01) Existing SuggestionsMirror rows drain seamlessly via SQL refill — no data wipe                                                                                    | ✓ VERIFIED | `maybe_schedule_refill` (preserved name for monkeypatch stability) at suggestions_service.py:370 now calls `refill_mirror_sql` (line 385); `_write_sql_refill_results_sync` INSERTs into existing schema                                              |
| 10  | (Plan 01) `tests/conftest.py` exposes `db_with_phase7` + `fresh_db` fixtures                                                                                                | ✓ VERIFIED | `tests/conftest.py:79` `def db_with_phase7(test_engine)` and `tests/conftest.py:121` `def fresh_db(tmp_path, monkeypatch)`                                                                                                                          |
| 11  | (SC#2, Plan 02) APScheduler cron `discovery_call_weekly` registered Sunday 03:00 UTC                                                                                        | ✓ VERIFIED | `app/services/sync_scheduler.py:148-150` `CronTrigger(day_of_week="sun", hour=3, minute=0, timezone="UTC")`                                                                                                                                          |
| 12  | (Plan 02) `discovery_call_weekly()` invokes Anthropic with purpose='discovery_weekly', writes 3-7 adaptive picks, resets counter + stamps timestamp on success              | ✓ VERIFIED | `app/services/suggestions_discovery.py:538` `async def discovery_call_weekly`; line 700 `purpose=DISCOVERY_PURPOSE` (= "discovery_weekly"); calls `_reset_plays_since_discovery_sync`; `WEEKLY_DISCOVERY_PICKS_RANGE = (3, 7)` at line 67           |
| 13  | (Plan 02) Startup catch-up gate placed OUTSIDE `with Session(engine) as session:` block (W9 invariant)                                                                      | ✓ VERIFIED | `app/services/sync_scheduler.py:220-267` — gate sits in its own `try` block after the outer with-Session in `start_scheduler` closes; verified by `test_catch_up_gate_does_not_open_nested_session`                                                  |
| 14  | (Plan 02) `compute_discovery_eligible()` excludes tracks in mirror, hard negatives, 14-day SuggestionHistory; tracks unplayed in 90+ days (D-A1)                            | ✓ VERIFIED | `app/services/suggestions_discovery.py:269` `_read_discovery_eligible_sync` SQL excludes all three; `last_viewed_at < cutoff` strict-less-than (W12)                                                                                                  |
| 15  | (Plan 02) LLM failure path writes LLMUsage with error_text populated; counter NOT reset, last_discovery_run_at NOT stamped (D-C3)                                           | ✓ VERIFIED | `_log_discovery_failure_sync` writes `error_text` (line 526 in suggestions_discovery.py); D-C3 invariant verified by `TestDiscoveryCallWeekly` failure tests; 13/13 pass live                                                                          |
| 16  | (Plan 02) `schedule_discovery_call_weekly()` registered in `app/main.py::lifespan` AFTER `start_scheduler()` and after `schedule_soft_negative_sweep()`                     | ✓ VERIFIED | `app/main.py:257 start_scheduler()` → `:264 schedule_soft_negative_sweep()` → `:270 schedule_discovery_call_weekly()`                                                                                                                                |
| 17  | (Plan 02 / SC#6) `DISCOVERY_MAX_TOKENS_FLOOR >= 8000`; retry catches `MaxTokensTruncationError` SPECIFICALLY (Blocker #5 bounded retry surface)                              | ✓ VERIFIED | `app/services/suggestions_discovery.py:78` `DISCOVERY_MAX_TOKENS_FLOOR = 8000`; line 704 `except MaxTokensTruncationError as exc:` — only this type triggers retry; line 714 `current_max_tokens *= 2`                                                |
| 18  | (Plan 02) W12 boundary — track at exactly `(now - 90 days).isoformat()` excluded (strict `<`, not `<=`)                                                                     | ✓ VERIFIED | `_read_discovery_eligible_sync` uses `t.last_viewed_at < :cutoff` (strict less-than); test `test_track_at_exactly_90_day_threshold_is_excluded` lives in `TestDiscoveryEligible`                                                                       |
| 19  | (Plan 03 / SC#3) `DAILY_COST_BUDGET_USD` renamed to `WEEKLY_DISCOVERY_BUDGET_USD = 0.50`; no remaining references to old name in codebase                                   | ✓ VERIFIED | `grep -rn 'DAILY_COST_BUDGET_USD' app/` → 0 matches; `app/services/llm_cost_breaker.py:48` `WEEKLY_DISCOVERY_BUDGET_USD = 0.50`                                                                                                                       |
| 20  | (Plan 03) Cost meter card shows "Anthropic spend this week" with rolling 7-day aggregation from LLMUsage                                                                    | ✓ VERIFIED | `app/templates/partials/llm_cost_meter.html:7` `<h2>Anthropic spend this week</h2>`; `app/routers/pages.py:328-333` `seven_days_ago_iso = ... - _td(days=7); LLMUsage.called_at >= seven_days_ago_iso`                                                  |
| 21  | (Plan 03) Template vars renamed: `today_calls → this_week_calls`, `today_cost_usd → this_week_cost_usd`, `daily_budget_usd → weekly_budget_usd`                              | ✓ VERIFIED | `pages.py:384-387` renders all 3 new names; old names absent from template; template uses `this_week_calls`, `this_week_cost_usd`, `weekly_budget_usd`                                                                                                |
| 22  | (Plan 03 / SC#6) AST regression test forbids `max_tokens=2000` literal in suggestions_discovery.py                                                                          | ✓ VERIFIED | `tests/test_suggestions_discovery.py:1293` `test_no_hardcoded_max_tokens_2000_in_suggestions_discovery` — PASSES live                                                                                                                                  |
| 23  | (Plan 03) AST tests forbid `refill_suggestions_queue` and `SUGGESTIONS_RANK_MAX_TOKENS` from re-entering suggestions_service.py                                              | ✓ VERIFIED | `tests/test_suggestions_service.py:889` + `:954` — all 4 `TestNoLegacyLlmRefillRefs` tests PASS live (`pytest` run captured)                                                                                                                          |
| 24  | (Plan 03) Belt-and-suspenders: `DAILY_QUOTA`, `BURST_LIMIT`, `BURST_WINDOW_SECONDS`, `DEBOUNCE_SECONDS` constants unchanged in llm_cost_breaker.py                            | ✓ VERIFIED | `import` test confirms `DAILY_QUOTA=50, BURST_LIMIT=5, DEBOUNCE_SECONDS=30`; pinned by `TestPhase71BudgetRename` (3 tests)                                                                                                                              |
| 25  | (Plan 03) "Caching not engaged" warning text replaced with neutral cold-start copy in llm_cost_meter.html                                                                    | ✓ VERIFIED | Template line 19 `— weekly cadence cold-starts the cache by design`; old "system prompt may be below 2048 tokens" string absent (`grep -c` → 0)                                                                                                       |
| 26  | (SC#3) Daily LLM cost for steady-state listening: $0.00 (no LLM in hot path)                                                                                                 | ✓ VERIFIED | `refill_mirror_sql` contains no Anthropic call; `RefillResult.breaker_tripped=False` always (line 763); AST guard `test_no_anthropic_call_in_suggestions_service` PASSES                                                                                |
| 27  | (SC#4) Mirror repopulated to target (default 30) within seconds of any drain                                                                                                 | ✓ VERIFIED | `SUGGESTIONS_TARGET_SIZE = 30` (suggestions_service.py:66); `refill_mirror_sql` is synchronous SQL (sub-second on a ~10K track library — no network calls); `maybe_schedule_refill` invokes immediately via `await refill_mirror_sql(target=target)` |

**Score:** 27/27 truths verified

### Required Artifacts

| Artifact                                            | Expected                                                                                       | Status     | Details                                                                                                                                                                          |
| --------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `app/models/vibe.py`                                | `class DiscoveryState` SQLModel single-row id=1 table                                          | ✓ VERIFIED | Line 208; fields `plays_since_last_discovery`, `last_discovery_run_at`; default id=1 per SetupState pattern                                                                       |
| `app/models/llm_usage.py`                           | `LLMUsage.error_text: Optional[str]`                                                           | ✓ VERIFIED | Line 33; nullable; populated only on failure paths (capped 500 chars by callers)                                                                                                  |
| `app/database.py`                                   | `DiscoveryState` registered in init_db before `create_all`; LLMUsage error_text ALTER migration | ✓ VERIFIED | Line 212 imports DiscoveryState; lines 152-164 PRAGMA-guarded `ALTER TABLE llmusage ADD COLUMN error_text TEXT`                                                                   |
| `app/services/anthropic_client.py`                  | `MaxTokensTruncationError` raised on stop_reason='max_tokens'                                  | ✓ VERIFIED | Class at line 59; raise at line 250 inside `if stop_reason == "max_tokens":` branch; LLMUsage row logged BEFORE raise (line 227) for cost accuracy                                |
| `app/services/suggestions_service.py`               | `refill_mirror_sql` replaces removed `refill_suggestions_queue`; vibe pool helpers filter is_active=1 | ✓ VERIFIED | `refill_mirror_sql` at line 721; both vibe pool helpers carry `is_active = 1` (lines 552 + 590); zero matches for removed names                                                  |
| `app/services/suggestions_discovery.py`             | `discovery_call_weekly`, `compute_discovery_eligible`, `compute_adaptive_pick_count`, `increment_plays_since_last_discovery`, `get_state`, `DiscoveryServiceStatus` | ✓ VERIFIED | All 6 symbols importable; module ~1000 lines; constants `DISCOVERY_MAX_TOKENS_FLOOR=8000`, `WEEKLY_DISCOVERY_PICKS_RANGE=(3,7)`, `DISCOVERY_PURPOSE="discovery_weekly"`                |
| `app/services/sync_scheduler.py`                    | `schedule_discovery_call_weekly()` + startup catch-up gate OUTSIDE with-Session                | ✓ VERIFIED | Line 119 schedule function with CronTrigger sun/03:00/UTC; lines 224-267 catch-up gate in own try block after outer with-Session closes                                          |
| `app/main.py`                                       | `schedule_discovery_call_weekly()` called in lifespan after `start_scheduler()`                | ✓ VERIFIED | Lines 257 → 264 → 270 (start_scheduler → soft_negative_sweep → discovery_call_weekly)                                                                                              |
| `app/services/event_handlers.py`                    | `handle_track_played` increments `plays_since_last_discovery` via lazy import + try/except      | ✓ VERIFIED | Lines 281-289; lazy `from app.services import suggestions_discovery`; best-effort exception swallow with logger.exception                                                          |
| `app/services/llm_cost_breaker.py`                  | `WEEKLY_DISCOVERY_BUDGET_USD = 0.50`; DAILY_COST_BUDGET_USD removed                            | ✓ VERIFIED | Line 48; old name absent project-wide                                                                                                                                              |
| `app/routers/pages.py`                              | `read_settings` aggregates LLMUsage over rolling 7-day window; template context renamed         | ✓ VERIFIED | Lines 315-392; `seven_days_ago_iso` computed; context vars `this_week_calls`, `this_week_cost_usd`, `weekly_budget_usd`                                                            |
| `app/templates/partials/llm_cost_meter.html`        | "Anthropic spend this week" heading + variable refs + neutral cold-start copy                  | ✓ VERIFIED | 32-line template; all 3 weekly vars rendered; `weekly cadence cold-starts the cache by design`                                                                                    |
| `tests/conftest.py`                                 | `db_with_phase7` + `fresh_db` fixtures promoted                                                | ✓ VERIFIED | Lines 79 + 121                                                                                                                                                                     |
| `tests/test_suggestions_service.py`                 | `TestNoLegacyLlmRefillRefs` (4 AST regression tests)                                            | ✓ VERIFIED | Class at line 859; 4 tests; all PASS live                                                                                                                                          |
| `tests/test_suggestions_discovery.py`               | `TestSuggestionsDiscoveryMaxTokensGuard` (3 AST regression tests) + `TestDiscoveryCallWeekly` (13 tests) | ✓ VERIFIED | Classes at lines 1274 + lower; all PASS live (3/3 + 13/13)                                                                                                                         |

All 15 artifacts: ✓ VERIFIED (exists, substantive, wired, data flows).

### Key Link Verification

| From                                                                              | To                                                              | Via                                                                          | Status     | Details                                                                                                                                                |
| --------------------------------------------------------------------------------- | --------------------------------------------------------------- | ---------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `event_handlers.py::handle_track_played`                                          | `suggestions_service.maybe_schedule_refill`                     | `await suggestions_service.maybe_schedule_refill()` (event_handlers.py:269) | ✓ WIRED    | Single call site; symbol name preserved for ~6 monkeypatch sites in tests                                                                              |
| `suggestions_service.maybe_schedule_refill`                                       | `refill_mirror_sql`                                             | `await refill_mirror_sql(target=target)` (suggestions_service.py:385)        | ✓ WIRED    | Deficit-gated; no LLM call                                                                                                                              |
| `refill_mirror_sql`                                                               | `TrackVibe.distance` JOIN `Vibe.is_active=1`                    | `ORDER BY tv.distance ASC` + `WHERE v.is_active = 1` (suggestions_service.py:590) | ✓ WIRED    | Both vibe pool helpers carry the symmetric filter                                                                                                       |
| `event_handlers.py::handle_track_played`                                          | `suggestions_discovery.increment_plays_since_last_discovery`    | lazy import + try/except (event_handlers.py:281-289)                          | ✓ WIRED    | Best-effort hook; failure does not break primary handler                                                                                                |
| `anthropic_client.AnthropicClient.call_with_structured_output`                    | `MaxTokensTruncationError`                                      | `raise MaxTokensTruncationError(...)` on stop_reason=='max_tokens' (line 250) | ✓ WIRED    | LLMUsage logged BEFORE raise (line 227)                                                                                                                  |
| `LLMUsage.error_text` column                                                      | `_log_discovery_failure_sync` writes                            | `error_text=` keyword in INSERT row (suggestions_discovery.py:526)           | ✓ WIRED    | Capped 500 chars                                                                                                                                        |
| `app/main.py::lifespan`                                                           | `schedule_discovery_call_weekly`                                | import + call after `start_scheduler()` (main.py:268-270)                    | ✓ WIRED    | Mirrors `schedule_soft_negative_sweep` ordering                                                                                                          |
| `sync_scheduler.schedule_discovery_call_weekly`                                   | `suggestions_discovery.discovery_call_weekly`                   | lazy import + `add_job(trigger=CronTrigger(day_of_week='sun', hour=3))` (sync_scheduler.py:148-149) | ✓ WIRED    | `replace_existing=True` for idempotent restart behavior                                                                                                  |
| `discovery_call_weekly` retry loop                                                | `MaxTokensTruncationError`                                      | `except MaxTokensTruncationError as exc:` (suggestions_discovery.py:704)     | ✓ WIRED    | Only this type triggers retry; generic exceptions hit failure path on first attempt                                                                     |
| `discovery_call_weekly`                                                           | `_reset_plays_since_discovery_sync`                             | called on successful completion path                                          | ✓ WIRED    | Stamps `last_discovery_run_at` + zeroes counter                                                                                                          |
| `sync_scheduler.start_scheduler` catch-up                                         | `discovery_call_weekly`                                         | `asyncio.create_task(_delayed_catch_up_discovery())` (sync_scheduler.py:260)  | ✓ WIRED    | 10s sleep + lazy re-import so test monkeypatches work                                                                                                    |
| `llm_cost_breaker.WEEKLY_DISCOVERY_BUDGET_USD`                                    | `pages.py::read_settings`                                       | import + template context `weekly_budget_usd` (pages.py:324, :387)            | ✓ WIRED    | Renders in `llm_cost_meter.html`                                                                                                                          |
| `pages.py::read_settings` (7-day window)                                          | `llm_cost_meter.html`                                           | `this_week_calls`, `this_week_cost_usd`, `weekly_budget_usd` template vars   | ✓ WIRED    | Heading + body rendered from these vars                                                                                                                  |

All 13 key links: ✓ WIRED.

### Data-Flow Trace (Level 4)

| Artifact                          | Data Variable                       | Source                                                                                                    | Produces Real Data | Status     |
| --------------------------------- | ----------------------------------- | --------------------------------------------------------------------------------------------------------- | ------------------ | ---------- |
| `llm_cost_meter.html`             | `this_week_calls`, `this_week_cost_usd`, `weekly_budget_usd` | `read_settings` queries `LLMUsage` rows with `called_at >= seven_days_ago_iso` (real SQL) | Yes                | ✓ FLOWING  |
| `refill_mirror_sql` writes        | `picks` list                         | `_read_top_n_for_vibe_sync` returns DB rows from `trackvibe` JOIN `track` JOIN `vibe`                     | Yes                | ✓ FLOWING  |
| `discovery_call_weekly` writes    | Anthropic response → `DiscoveryPicksResponse.picks` → SuggestionsMirror | `compute_discovery_eligible` (real SQL) + Anthropic API + `_write_discovery_picks_sync` (real INSERT) | Yes                | ✓ FLOWING  |
| `DiscoveryState` counter          | `plays_since_last_discovery`         | `_increment_plays_since_discovery_sync` called from `handle_track_played` on every play                   | Yes                | ✓ FLOWING  |
| `LLMUsage.error_text` rows        | `error_text` string                  | `_log_discovery_failure_sync` writes on every failure path in `discovery_call_weekly`                     | Yes                | ✓ FLOWING  |

All data flows: ✓ FLOWING.

### Behavioral Spot-Checks

| Behavior                                                  | Command                                                                                                          | Result                                                  | Status |
| --------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | ------ |
| 7 AST regression guards pass                              | `.venv/bin/python -m pytest tests/test_suggestions_service.py::TestNoLegacyLlmRefillRefs tests/test_suggestions_discovery.py::TestSuggestionsDiscoveryMaxTokensGuard -v` | 7 passed                                                | ✓ PASS |
| 13 `TestDiscoveryCallWeekly` tests pass (full retry surface) | `.venv/bin/python -m pytest tests/test_suggestions_discovery.py::TestDiscoveryCallWeekly -v`                    | 13 passed                                               | ✓ PASS |
| `handle_track_played` counter increment tests pass         | `.venv/bin/python -m pytest tests/test_event_handlers.py::TestHandleTrackPlayedDiscoveryCounterIncrement`        | 2 passed                                                | ✓ PASS |
| All 5 plan-touched test files pass in isolation           | `.venv/bin/python -m pytest tests/test_suggestions_service.py tests/test_suggestions_discovery.py tests/test_anthropic_client.py tests/test_llm_cost_breaker.py tests/test_pages_settings.py` | 114 passed                                              | ✓ PASS |
| `WEEKLY_DISCOVERY_BUDGET_USD = 0.50`; `DAILY_COST_BUDGET_USD` not importable | `python -c "from app.services.llm_cost_breaker import WEEKLY_DISCOVERY_BUDGET_USD; ..."` | 0.5; ImportError as expected                            | ✓ PASS |
| `MaxTokensTruncationError`, `refill_mirror_sql`, `discovery_call_weekly`, `DiscoveryState`, `LLMUsage.error_text`, `schedule_discovery_call_weekly` all importable | python `import` smoke test                                                                                       | All 6 symbols import; deleted names raise ImportError    | ✓ PASS |
| Sunday 03:00 UTC cron trigger string                       | `grep "day_of_week=\"sun\", hour=3, minute=0, timezone=\"UTC\"" app/services/sync_scheduler.py`                  | Match at line 149                                       | ✓ PASS |

All behavioral spot-checks: ✓ PASS.

### Requirements Coverage

| Requirement                            | Source Plan | Description                                                                                                   | Status      | Evidence                                                                                                                       |
| -------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------ |
| SUGG-04 (REWORKED)                     | Phase-level | "LLM ranks shortlist on every refill" semantic is replaced; SQL hot path + weekly LLM discovery               | ✓ SATISFIED | Per-event LLM refill fully removed (Plan 01); weekly LLM discovery wired (Plan 02); REQUIREMENTS.md line 134 records rework    |
| SUGG-12                                | Plan 01     | SQL hot-path refill against TrackVibe.distance, filter `Vibe.is_active = 1`, cross-vibe weighted; zero LLM    | ✓ SATISFIED | `refill_mirror_sql` (suggestions_service.py:721) + symmetric is_active filter on both vibe pool helpers + 11 new TestRefillMirrorSql tests |
| SUGG-13                                | Plan 02     | Weekly Anthropic discovery call Sunday 03:00 UTC; 3-7 picks adaptive; counter + last_run_at state             | ✓ SATISFIED | `discovery_call_weekly` (suggestions_discovery.py:538) + APScheduler registration (sync_scheduler.py:119) + lifespan wire (main.py:270) + 13 tests |
| SUGG-14                                | Plan 03     | Defensive `max_tokens` sizing; `MaxTokensTruncationError` typed exception + bounded retry; AST regression test | ✓ SATISFIED | `MaxTokensTruncationError` (anthropic_client.py:59) raised on truncation + bounded retry loop catching only this type (suggestions_discovery.py:704) + 3 AST guards (test_suggestions_discovery.py:1274) |

**No orphaned requirements:** Cross-referenced REQUIREMENTS.md line 284 "SUGG-04 (REWORKED), SUGG-12, SUGG-13, SUGG-14 | Phase 7.1 | Pending" — all 4 IDs declared in PLAN frontmatter are present; no additional Phase-7.1 IDs appear in REQUIREMENTS.md outside the plans.

**Pending status note:** All 4 requirements are still marked `[ ]` (Pending) in REQUIREMENTS.md. Per the GSD workflow, orchestrator marks them complete during commit; implementation evidence is fully in place.

### Anti-Patterns Found

| File                                       | Line | Pattern                                                                                                       | Severity | Impact                                                                                                                                                              |
| ------------------------------------------ | ---- | ------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `app/services/suggestions_service.py`      | 211  | Docstring says "Plan 01 placeholder for the Plex playlist creation step"                                       | ℹ️ Info  | Not a Phase 7.1 stub — this is a Phase 7 Plan 01 indirection helper (`_create_plex_suggestions_playlist`) that returns None to defer materialization to first refill. Test indirection only; verified by surrounding docstring and call sites |
| `app/services/suggestions_discovery.py`    | 674  | Comment mentions "placeholders" — describes test fake-client constructor arguments                              | ℹ️ Info  | Comment, not a stub. Production code uses real `_read_anthropic_credentials_sync`; tests monkeypatch the AnthropicClient class so the placeholder strings are inert |

**No BLOCKER or WARNING anti-patterns found.** Both Info-level findings are legitimate documentation of test indirection, not implementation gaps.

### Code Review Findings (from 07.1-REVIEW.md, surfaced for human awareness)

The plan-checker REVIEW.md identifies **0 critical, 4 warnings, 5 info** items — all phase contract checks PASS per reviewer. The 4 warnings are correctness edge cases (position-collision when mirror drained to position=0; misleading env-var imports; over-broad exception swallow on Anthropic config absence; SQL LIKE wildcard) — none block the Phase 7.1 goal but should be triaged in a follow-up. Documented in REVIEW.md for future plan/quick task work.

### Pre-Existing Test Failures (Deferred)

`tests/test_sync_scheduler.py`: 3 pre-existing failures (`test_triggers_auto_sync_when_no_prior_sync`, `test_shuts_down_without_error`, `test_updates_running_scheduler`) — root cause: `RuntimeError: no running event loop` from sync test bodies marked `@pytest.mark.asyncio`. Confirmed pre-existing via git stash baseline; documented in `deferred-items.md`. NOT caused or worsened by Phase 7.1.

Additionally, `deferred-items.md` documents 14 other pre-existing failures across sync/audio/chat/analysis test modules. None are caused by Phase 7.1.

### Human Verification Required

4 items need human testing on the deployed NAS:

1. **Settings page cost meter renders weekly framing**
   - What to do: GET `/settings` on the NAS after deploy
   - Expected: heading reads "Anthropic spend this week" (not "today"); shows $X / $0.50 budgeted; weekly cadence cold-start copy
   - Why human: Visual / HTML render verification

2. **First-deploy startup catch-up fires within 10 seconds**
   - What to do: Restart Composer container on NAS; tail logs
   - Expected: "scheduling catch-up discovery_call_weekly in 10s" log; LLMUsage row with `purpose='discovery_weekly'` appears; SuggestionsMirror gains 3-7 discovery picks; `DiscoveryState.last_discovery_run_at` stamped
   - Why human: Live end-to-end Anthropic + Plex + APScheduler integration

3. **Sunday 03:00 UTC cron actually fires**
   - What to do: Wait until next Sunday 03:00 UTC after deploy
   - Expected: New `discovery_call_weekly` LLMUsage row + new discovery picks in mirror
   - Why human: Time-based cron behavior

4. **Every TrackPlay webhook refills mirror via SQL with $0 LLM cost**
   - What to do: Play 8+ tracks in a row on Plex; watch `/settings` cost meter + LLMUsage table
   - Expected: LLMUsage row count does NOT increase; SuggestionsMirror stays ~30 rows; cost meter spend stays flat
   - Why human: Real Plex webhook → handler → SQL refill chain in production

### Gaps Summary

**No gaps.** All 27 must_haves verified against the live codebase. All 13 key links wired. All data flows. All 4 requirements (SUGG-04 REWORK, SUGG-12, SUGG-13, SUGG-14) satisfied. AST regression guards lock the architecture in place — verified live.

The only remaining work is **human verification on the NAS** of 4 user-observable behaviors that cannot be programmatically tested without a live deployment (HTML rendering, real Anthropic API call, time-based cron firing, live Plex webhook chain). The implementation itself is complete.

---

_Verified: 2026-05-15T22:59:43Z_
_Verifier: Claude (gsd-verifier)_
