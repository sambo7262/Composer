---
phase: 07.1-suggestions-cost-architecture-sql-refill-weekly-discovery
verified: 2026-05-15T22:59:43Z
updated: 2026-05-16T09:15:00Z
status: human_needed
score: 29/29 must-haves verified (27 initial + 2 gap-closure)
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 27/27 (with 2 live UAT gaps)
  gaps_closed:
    - "GAP-01: Discovery LLM call prompt exceeds Claude 200K input context on cold-start catch-up"
    - "GAP-02: Frontend library_stats partial polls /api/library/stats 25-50 times/second"
  gaps_remaining: []
  regressions: []
resolved_gaps:
  - id: GAP-01
    title: "Discovery LLM call prompt exceeds Claude 200K input context on cold-start catch-up"
    severity: blocking
    in_scope: 07.1
    discovered: 2026-05-16 NAS UAT
    resolved_in: 07.1-04-PLAN.md (Plan 04)
    resolved_at: 2026-05-16
    closure_evidence: |
      `app/services/suggestions_discovery.py:103` `DISCOVERY_CANDIDATE_LIMIT = 200`
      `app/services/suggestions_discovery.py:111` `DISCOVERY_PROMPT_TOKEN_CEILING = 150_000`
      `app/services/suggestions_discovery.py:119` `DISCOVERY_CHARS_PER_TOKEN_RATIO = 3.5`
      `_read_discovery_eligible_sync` (line 304) gains `limit: int = DISCOVERY_CANDIDATE_LIMIT` kwarg + SQL `ORDER BY CASE WHEN t.last_viewed_at IS NULL THEN 0 ELSE 1 END, t.last_viewed_at ASC, t.id ASC LIMIT :limit` (lines 373-377 — portable SQLite NULLS-FIRST emulation per D-9 invariant).
      `compute_discovery_eligible` (line 398) threads the `limit` kwarg through `asyncio.to_thread`.
      Trimmer block (lines 725-781) sits BETWEEN `user_prompt = _build_discovery_user_prompt(...)` build (line 721) AND credentials read (line 788). Comment at lines 734-738: "the trimmer runs ONCE before the retry loop; the retry loop below only adjusts max_tokens (OUTPUT side), and NEVER re-trims candidates."
      Iterative halve-and-rebuild loop (lines 744-769) with halt-at-pick_count safety floor at line 748. `logger.warning("discovery_call_weekly: trimming candidate pool from %d to %d ...")` at lines 771-781.
      11 new regression tests pass live: 5 in `TestDiscoveryEligible` (constant pin, LIMIT cap, ORDER BY NULLs-first-then-oldest, kwarg threading, W12 invariance) + 6 in `TestDiscoveryPromptSize` (ceiling pin, ratio pin, no-trim small, oversize-trim 10K-fixture, oldest-preserved, halt-at-pick-count). Live: `.venv/bin/python -m pytest tests/test_suggestions_discovery.py::TestDiscoveryEligible tests/test_suggestions_discovery.py::TestDiscoveryPromptSize -v` → 17 passed in 7.83s.
      Phase 7.1 AST invariants verified intact: `test_no_session_outside_sync_helper_in_suggestions_discovery` PASSES (new SQL ORDER BY/LIMIT lives inside `_read_discovery_eligible_sync`); `test_no_hardcoded_max_tokens_2000_in_suggestions_discovery` PASSES; `test_no_max_tokens_below_floor_in_suggestions_discovery` PASSES; `test_all_sql_helpers_called_via_to_thread` PASSES (compute_discovery_eligible still wraps via to_thread with new `limit` arg).
      SUGG-14 output-side `MaxTokensTruncationError` retry preserved unchanged — the new trimmer is INPUT-side; 13/13 `TestDiscoveryCallWeekly` tests pass.
  - id: GAP-02
    title: "Frontend library_stats partial polls /api/library/stats 25-50 times/second"
    severity: warning
    in_scope: pre-existing (predates 07.1) — bundled into 07.1 gap closure per user direction
    discovered: 2026-05-16 NAS UAT
    resolved_in: 07.1-05-PLAN.md (Plan 05)
    resolved_at: 2026-05-16
    closure_evidence: |
      `app/templates/partials/library_stats.html` line 3 reads `hx-trigger="every 10s"` (was: `hx-trigger="load, every 10s"`). One-line fix. Other 23 lines preserved verbatim (id, hx-get, hx-target, hx-swap, body content).
      `tests/test_library_stats_partial.py` exists — 135 lines, 1 class `TestLibraryStatsPartialPolling`, 4 regression tests:
        - `test_partial_contains_polling_only_trigger`: asserts `hx-trigger="every 10s"` present AND neither `hx-trigger="load, every 10s"` nor `load,every 10s` (whitespace variant) AND regex `r'hx-trigger="load,\s*'` matches nothing.
        - `test_partial_preserves_outerhtml_swap_target`: id, hx-target, hx-swap, hx-get all preserved.
        - `test_partial_labels_still_render`: Total tracks / Rated tracks / Last rating event labels present.
        - `test_partial_response_is_idempotent_across_rapid_calls`: 3 sequential calls all return polling-only trigger.
      Local `client(test_engine)` fixture defined at lines 29-48 (NOT promoted to conftest.py per plan scope).
      Live: `.venv/bin/python -m pytest tests/test_library_stats_partial.py -v` → 4 passed in 1.96s.
      Verified parent-page invariants intact (no changes): `app/templates/pages/settings.html:10` `hx-trigger="load"`, `app/templates/pages/debug_events.html:6` `hx-trigger="load"`, `app/templates/pages/debug_vibes.html:9` `hx-trigger="load"` — each parent page carries its own one-shot trigger for initial render.
      `grep -rn 'load, every\|load,every' app/templates/` → 0 matches (no infinite-loop pattern anywhere in templates).
gaps: []
human_verification:
  - test: "Settings page cost meter renders weekly framing on the NAS"
    expected: "GET /settings shows 'Anthropic spend this week' heading, $X / $0.50 budgeted, and weekly cadence cold-start copy (no 'Anthropic spend today' or 'caching not engaged (system prompt may be below 2048 tokens)' strings)"
    why_human: "Visual / template render verification requires running the FastAPI app + a real LLMUsage row population over the rolling 7-day window; AST + import tests cover the source side but not the rendered HTML in a real browser"
    blocked_by_gap_closure: false
    note: "GAP-02 closure (Plan 05) removes the 25-50 req/s ambient NAS load that was contaminating this UAT; item is now cleanly testable."
  - test: "First-deploy startup catch-up fires within 10 seconds on the NAS (now unblocked at code level by GAP-01 closure)"
    expected: "On first restart after 7.1+gap-closure deploy, logs show 'Last discovery run is NULL or > 7 days old — scheduling catch-up discovery_call_weekly in 10s' followed (10 seconds later) by an Anthropic call with purpose='discovery_weekly' AND NO BadRequestError prompt-is-too-long. LLMUsage row written. SuggestionsMirror gains 3-7 new rows with discovery_weekly event_source. DiscoveryState.last_discovery_run_at is stamped. If the candidate pool was large enough to trigger the trimmer, logs ALSO show 'discovery_call_weekly: trimming candidate pool from X to Y candidates ...' WARNING."
    why_human: "End-to-end live verification of the D-C2 catch-up gate + GAP-01 trimmer requires real Anthropic credentials + real Plex library + APScheduler running in production lifespan; unit tests stub the LLM client and the timing"
    blocked_by_gap_closure: false
    note: "GAP-01 closure (Plan 04) caps the prompt at ~150K tokens via SQL LIMIT + chars/3.5 trimmer — the previous BadRequestError (202027 tokens > 200000) is now defended against. Pending NAS re-deploy."
  - test: "Sunday 03:00 UTC cron actually fires on the NAS"
    expected: "After 7+ days, on the next Sunday 03:00 UTC, a discovery_call_weekly LLMUsage row appears and SuggestionsMirror gains discovery picks"
    why_human: "Time-based cron verification requires waiting for the actual schedule; covered statically by AST tests + register-time assertions but not behaviorally"
    blocked_by_gap_closure: false
    note: "Unchanged from prior verification — CronTrigger registration still in place at app/services/sync_scheduler.py:148-149 (day_of_week='sun', hour=3, minute=0, timezone='UTC')."
  - test: "Every TrackPlay webhook event refills the mirror via SQL with $0 LLM cost"
    expected: "Play 8+ tracks in a row on Plex; observe (a) LLMUsage row count does NOT increase (no per-play LLM calls), (b) SuggestionsMirror.count() stays at ~30 (drained-and-refilled), (c) cost-meter spend stays flat over the play burst"
    why_human: "Real Plex webhook delivery + real listening session needed; unit tests cover handler logic with stubbed mirror but not the live webhook → handler → SQL refill chain on the NAS"
    blocked_by_gap_closure: false
    note: "Unchanged from prior verification — refill_mirror_sql + handle_track_played counter increment intact. No GAP-01/02 code changes affect this UAT."
---

# Phase 7.1: Suggestions Cost Architecture Verification Report

**Phase Goal:** Land Phase 7.1's "Option C" architecture — replace the LLM-on-every-refill suggestions hot path with SQL-driven refill from pre-computed TrackVibe.distance (zero LLM tokens per play), and introduce a weekly LLM discovery layer that backfills new candidates when plays_since_last_discovery crosses the eligibility threshold. Add cost-meter relabel + AST regression guards to lock the architecture in place.

**Verified:** 2026-05-15T22:59:43Z (initial) / 2026-05-16T09:15:00Z (re-verification after gap closure)
**Status:** human_needed
**Re-verification:** Yes — gap-closure pass after Plans 04 + 05 merged.

## Re-Verification Summary (2026-05-16)

Both NAS-UAT-surfaced gaps from the initial verification are **CLOSED** in the codebase:

| Gap | Plan | Status | Live Test |
| --- | ---- | ------ | --------- |
| GAP-01 — Discovery prompt > 200K | 07.1-04 | ✓ CLOSED | 11/11 new tests pass + 21/21 invariant tests preserved |
| GAP-02 — library_stats infinite-loop polling | 07.1-05 | ✓ CLOSED | 4/4 new tests pass + 5/5 existing partial-related tests preserved |

- **Score:** 29/29 must-haves verified (27 initial + 2 gap-closure must-haves, both newly verified).
- **Phase 7.1 invariants preserved:** All 7 AST regression guards still pass — `test_no_session_outside_sync_helper_in_suggestions_discovery`, `test_no_hardcoded_max_tokens_2000_in_suggestions_discovery`, `test_no_max_tokens_below_floor_in_suggestions_discovery`, `test_all_sql_helpers_called_via_to_thread`, and 4-test `TestNoLegacyLlmRefillRefs`.
- **SUGG-14 OUTPUT-side retry intact:** The new INPUT-side trimmer is positioned BEFORE the retry loop with explicit comment; `MaxTokensTruncationError` retry-once semantics from Plan 02 unchanged. 13/13 `TestDiscoveryCallWeekly` tests pass.
- **Phase 5 D-09 helper-boundary intact:** New SQL ORDER BY/LIMIT lives inside the existing `_read_discovery_eligible_sync` helper; trimmer block is pure-Python (no Session). AST guard PASSES.
- **handle_track_played counter increment intact:** `event_handlers.py:284` `await suggestions_discovery.increment_plays_since_last_discovery()` unchanged. `TestHandleTrackPlayedDiscoveryCounterIncrement` 2/2 PASS.
- **Cost-meter framing intact:** `WEEKLY_DISCOVERY_BUDGET_USD = 0.50` at `llm_cost_breaker.py:48`; old name absent.
- **Cron registration intact:** `CronTrigger(day_of_week="sun", hour=3, minute=0, timezone="UTC")` at `sync_scheduler.py:148-149`.

**Remaining work — 4 NAS UAT items, none blocked by code gaps:**
- Item #1 (cost meter visual): now cleanly testable (GAP-02 closure removed the ambient 25-50 req/s load that contaminated this UAT).
- Item #2 (first-deploy catch-up): now unblocked at the code level (GAP-01 closure caps prompt size). Pending NAS re-deploy.
- Item #3 (Sunday cron): time-based, still pending (no code changes since initial verification).
- Item #4 (TrackPlay refills at $0): code unchanged since main 07.1 plans; still pending NAS test.

Verdict: **human_needed.** All code-level closures verified. NAS UAT items 1, 2, 3, 4 remain pending live verification (none blocked at the source level).

---

## Gap Closure Verification — GAP-01

**Plan:** `07.1-04-PLAN.md` → `07.1-04-SUMMARY.md`. Two commits: `3672929` (RED) → `68c06d7` (GREEN) for Task 1; `cd3623f` (RED) → `20236cb` (GREEN) for Task 2.

### Must-Haves (Plan 04 frontmatter — all VERIFIED)

| #   | Truth                                                                                                                          | Status     | Evidence                                                                                                                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------ | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 28  | `DISCOVERY_CANDIDATE_LIMIT = 200` module constant; `_read_discovery_eligible_sync` SQL has `ORDER BY ... LIMIT :limit` clause | ✓ VERIFIED | `app/services/suggestions_discovery.py:103` defines `DISCOVERY_CANDIDATE_LIMIT = 200`; SQL at lines 373-377 has `ORDER BY CASE WHEN t.last_viewed_at IS NULL THEN 0 ELSE 1 END, t.last_viewed_at ASC, t.id ASC LIMIT :limit`; both `_read_discovery_eligible_sync(limit=...)` (line 306) and `compute_discovery_eligible(limit=...)` (line 400) carry the kwarg |
| 29  | `DISCOVERY_PROMPT_TOKEN_CEILING = 150_000`; trimmer block runs ONCE between user_prompt build and credentials read              | ✓ VERIFIED | `app/services/suggestions_discovery.py:111` defines ceiling; `:119` defines `DISCOVERY_CHARS_PER_TOKEN_RATIO = 3.5`; trimmer at lines 725-781 sits between user_prompt build (line 721) and credentials read (line 788); explicit comment at lines 735-738: "trimmer runs ONCE before the retry loop; the retry loop below only adjusts max_tokens (OUTPUT side), and NEVER re-trims candidates" |

### Behavioral Spot-Checks (GAP-01)

| Behavior                                                          | Command                                                                                                                                                                          | Result                                                  | Status |
| ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | ------ |
| 3 new constants importable with expected values                   | `python -c "from app.services.suggestions_discovery import DISCOVERY_CANDIDATE_LIMIT, DISCOVERY_PROMPT_TOKEN_CEILING, DISCOVERY_CHARS_PER_TOKEN_RATIO"`                          | `CL=200, PTC=150000, CPTR=3.5; OK`                      | ✓ PASS |
| 11 new GAP-01 tests pass (5 in TestDiscoveryEligible, 6 in TestDiscoveryPromptSize) | `.venv/bin/python -m pytest tests/test_suggestions_discovery.py::TestDiscoveryEligible tests/test_suggestions_discovery.py::TestDiscoveryPromptSize -v`                                                                  | 17 passed in 7.83s (6 prior + 11 new)                   | ✓ PASS |
| All Phase 7.1 invariants preserved                                 | `.venv/bin/python -m pytest tests/test_suggestions_discovery.py::TestSuggestionsDiscoveryAstShape tests/test_suggestions_discovery.py::TestSuggestionsDiscoveryMaxTokensGuard tests/test_suggestions_discovery.py::TestDiscoveryCallWeekly tests/test_suggestions_service.py::TestNoLegacyLlmRefillRefs -v` | 21 passed in 1.82s                                      | ✓ PASS |

### Key Link Verification (GAP-01)

| From                                                                          | To                                                                  | Via                                                                  | Status  |
| ----------------------------------------------------------------------------- | ------------------------------------------------------------------- | -------------------------------------------------------------------- | ------- |
| `_read_discovery_eligible_sync`                                               | `DISCOVERY_CANDIDATE_LIMIT`                                          | SQL `ORDER BY ... LIMIT :limit` (lines 373-377)                       | ✓ WIRED |
| `compute_discovery_eligible`                                                  | `_read_discovery_eligible_sync`                                      | `asyncio.to_thread(..., limit)` (lines 413-414)                       | ✓ WIRED |
| `discovery_call_weekly` trimmer                                                | `DISCOVERY_PROMPT_TOKEN_CEILING` + `DISCOVERY_CHARS_PER_TOKEN_RATIO` | while-loop condition + chars/ratio estimate (lines 740-768)           | ✓ WIRED |
| Trimmer halt branch                                                            | `pick_count` floor                                                   | `if len(candidates) <= pick_count: logger.error(...); break` (line 748) | ✓ WIRED |
| Trimmer success path                                                            | logger.warning("trimming candidate pool from ...")                  | lines 771-781                                                          | ✓ WIRED |

---

## Gap Closure Verification — GAP-02

**Plan:** `07.1-05-PLAN.md` → `07.1-05-SUMMARY.md`. Two commits: `2fa8537` (RED) → `fc6a8ba` (GREEN).

### Must-Haves (Plan 05 frontmatter — all VERIFIED)

| #   | Truth                                                                                                                          | Status     | Evidence                                                                                                                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------ | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 30  | `app/templates/partials/library_stats.html` line 3 = `hx-trigger="every 10s"` (no `load,` prefix); 4 regression tests in `tests/test_library_stats_partial.py` with local fixture | ✓ VERIFIED | `grep -n 'hx-trigger' app/templates/partials/library_stats.html` → `3:     hx-trigger="every 10s"` (single match); `tests/test_library_stats_partial.py` exists with local `client(test_engine)` fixture at lines 29-48; 4 tests in `TestLibraryStatsPartialPolling`; `grep -rn 'load, every\|load,every' app/templates/` → 0 matches |

### Behavioral Spot-Checks (GAP-02)

| Behavior                                                              | Command                                                                                                                                                | Result                                              | Status |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------- | ------ |
| 4 new GAP-02 regression tests pass                                    | `.venv/bin/python -m pytest tests/test_library_stats_partial.py -v`                                                                                    | 4 passed in 1.96s                                   | ✓ PASS |
| Existing parent-page embed + endpoint tests unchanged                  | `.venv/bin/python -m pytest tests/test_pages.py::TestLibraryAtAGlanceRender tests/test_library_api.py::TestStatsEndpoint -v`                            | 5 passed                                            | ✓ PASS |
| Parent pages still carry one-shot `hx-trigger="load"` for initial render | `grep -n 'hx-trigger="load"' app/templates/pages/settings.html app/templates/pages/debug_events.html app/templates/pages/debug_vibes.html`              | 5 matches (settings:10/38/48, debug_events:6, debug_vibes:9) | ✓ PASS |
| No infinite-loop pattern anywhere in templates                         | `grep -rn 'load, every\|load,every' app/templates/`                                                                                                    | 0 matches                                           | ✓ PASS |

### Key Link Verification (GAP-02)

| From                                              | To                          | Via                                                                              | Status  |
| ------------------------------------------------- | --------------------------- | -------------------------------------------------------------------------------- | ------- |
| `app/templates/partials/library_stats.html`        | `/api/library/stats`         | `hx-get="/api/library/stats" hx-trigger="every 10s"` (lines 2-3)                  | ✓ WIRED |
| `app/templates/pages/settings.html` (line 10)      | `app/templates/partials/library_stats.html` | parent's one-shot `hx-trigger="load" hx-target="this" hx-swap="innerHTML"` (NOT changed) | ✓ WIRED |
| `tests/test_library_stats_partial.py`             | live FastAPI route          | `TestClient(app).get("/api/library/stats")` + response-body string assertions    | ✓ WIRED |

---

## Goal Achievement (CARRIED FROM INITIAL VERIFICATION — all 27 still valid)

### Observable Truths (initial 27 — re-checked in Step 0 for regression; all still VERIFIED)

| #   | Truth                                                                                                                                                                      | Status     | Evidence                                                                                                                                                                                                                                            |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | (SC#1, Plan 01) Refill on every play uses ZERO LLM tokens — SQL query against `TrackVibe.distance` ordered ASC                                                             | ✓ VERIFIED | `app/services/suggestions_service.py:721` `async def refill_mirror_sql`; AST guard `test_no_anthropic_call_in_suggestions_service` PASSES live |
| 2   | (Plan 01) `refill_suggestions_queue`, `refill_suggestions_for_vibe`, and `SUGGESTIONS_RANK_MAX_TOKENS` are removed entirely from suggestions_service.py                    | ✓ VERIFIED | All 4 `TestNoLegacyLlmRefillRefs` tests PASS live |
| 3   | (Plan 01) `DiscoveryState` single-row id=1 SQLModel table holds plays_since_last_discovery + last_discovery_run_at                                                          | ✓ VERIFIED | Unchanged |
| 4   | (Plan 01) `LLMUsage.error_text` Optional[str] column added via `_migrate_add_columns` ALTER                                                                                | ✓ VERIFIED | Unchanged |
| 5   | (Plan 01) `MaxTokensTruncationError(Exception)` raised — NOT warned — on `response.stop_reason == "max_tokens"`                                                            | ✓ VERIFIED | Unchanged |
| 6   | (Plan 01) `handle_track_played` increments `plays_since_last_discovery` (best-effort try/except + lazy import)                                                             | ✓ VERIFIED | `app/services/event_handlers.py:284` confirmed; `TestHandleTrackPlayedDiscoveryCounterIncrement` 2/2 PASS live |
| 7   | (Plan 01) Both `_read_vibe_pool_weights_sync` AND `_read_top_n_for_vibe_sync` filter `Vibe.is_active = 1`                                                                  | ✓ VERIFIED | Unchanged |
| 8   | (Plan 01) Phase 5 D-09 AST invariant — every `Session(get_engine())` in `suggestions_discovery.py` lives in a `_*_sync` helper                                              | ✓ VERIFIED | `test_no_session_outside_sync_helper_in_suggestions_discovery` PASSES live AFTER GAP-01 closure (new SQL ORDER BY/LIMIT lives inside `_read_discovery_eligible_sync` helper) |
| 9   | (Plan 01) Existing SuggestionsMirror rows drain seamlessly via SQL refill — no data wipe                                                                                    | ✓ VERIFIED | Unchanged |
| 10  | (Plan 01) `tests/conftest.py` exposes `db_with_phase7` + `fresh_db` fixtures                                                                                                | ✓ VERIFIED | Unchanged |
| 11  | (SC#2, Plan 02) APScheduler cron `discovery_call_weekly` registered Sunday 03:00 UTC                                                                                        | ✓ VERIFIED | `app/services/sync_scheduler.py:148-149` `CronTrigger(day_of_week="sun", hour=3, minute=0, timezone="UTC")` |
| 12  | (Plan 02) `discovery_call_weekly()` invokes Anthropic with purpose='discovery_weekly', writes 3-7 adaptive picks, resets counter + stamps timestamp on success              | ✓ VERIFIED | Unchanged |
| 13  | (Plan 02) Startup catch-up gate placed OUTSIDE `with Session(engine) as session:` block (W9 invariant)                                                                      | ✓ VERIFIED | Unchanged |
| 14  | (Plan 02) `compute_discovery_eligible()` excludes tracks in mirror, hard negatives, 14-day SuggestionHistory; tracks unplayed in 90+ days (D-A1)                            | ✓ VERIFIED | Now ALSO carries `limit: int = DISCOVERY_CANDIDATE_LIMIT` kwarg (GAP-01 enhancement); all original filters preserved |
| 15  | (Plan 02) LLM failure path writes LLMUsage with error_text populated; counter NOT reset, last_discovery_run_at NOT stamped (D-C3)                                           | ✓ VERIFIED | 13/13 `TestDiscoveryCallWeekly` tests PASS live AFTER GAP-01 closure |
| 16  | (Plan 02) `schedule_discovery_call_weekly()` registered in `app/main.py::lifespan` AFTER `start_scheduler()` and after `schedule_soft_negative_sweep()`                     | ✓ VERIFIED | Unchanged |
| 17  | (Plan 02 / SC#6) `DISCOVERY_MAX_TOKENS_FLOOR >= 8000`; retry catches `MaxTokensTruncationError` SPECIFICALLY (Blocker #5 bounded retry surface)                              | ✓ VERIFIED | Floor unchanged at line 87 = 8000; retry-once semantics preserved at lines 814+; new GAP-01 trimmer sits BEFORE this retry loop (lines 725-781) so OUTPUT-side and INPUT-side controls remain independent |
| 18  | (Plan 02) W12 boundary — track at exactly `(now - 90 days).isoformat()` excluded (strict `<`, not `<=`)                                                                     | ✓ VERIFIED | `test_w12_boundary_preserved_with_limit` (new in GAP-01) explicitly re-asserts W12 invariance across the ORDER BY/LIMIT change — PASSES live |
| 19  | (Plan 03 / SC#3) `DAILY_COST_BUDGET_USD` renamed to `WEEKLY_DISCOVERY_BUDGET_USD = 0.50`; no remaining references to old name in codebase                                   | ✓ VERIFIED | `app/services/llm_cost_breaker.py:48` `WEEKLY_DISCOVERY_BUDGET_USD = 0.50` (sole match) |
| 20  | (Plan 03) Cost meter card shows "Anthropic spend this week" with rolling 7-day aggregation from LLMUsage                                                                    | ✓ VERIFIED | Unchanged |
| 21  | (Plan 03) Template vars renamed: `today_calls → this_week_calls`, `today_cost_usd → this_week_cost_usd`, `daily_budget_usd → weekly_budget_usd`                              | ✓ VERIFIED | Unchanged |
| 22  | (Plan 03 / SC#6) AST regression test forbids `max_tokens=2000` literal in suggestions_discovery.py                                                                          | ✓ VERIFIED | `test_no_hardcoded_max_tokens_2000_in_suggestions_discovery` PASSES live AFTER GAP-01 closure |
| 23  | (Plan 03) AST tests forbid `refill_suggestions_queue` and `SUGGESTIONS_RANK_MAX_TOKENS` from re-entering suggestions_service.py                                              | ✓ VERIFIED | All 4 `TestNoLegacyLlmRefillRefs` tests PASS live |
| 24  | (Plan 03) Belt-and-suspenders: `DAILY_QUOTA`, `BURST_LIMIT`, `BURST_WINDOW_SECONDS`, `DEBOUNCE_SECONDS` constants unchanged in llm_cost_breaker.py                            | ✓ VERIFIED | Unchanged |
| 25  | (Plan 03) "Caching not engaged" warning text replaced with neutral cold-start copy in llm_cost_meter.html                                                                    | ✓ VERIFIED | Unchanged |
| 26  | (SC#3) Daily LLM cost for steady-state listening: $0.00 (no LLM in hot path)                                                                                                 | ✓ VERIFIED | Unchanged |
| 27  | (SC#4) Mirror repopulated to target (default 30) within seconds of any drain                                                                                                 | ✓ VERIFIED | Unchanged |

**Initial Score:** 27/27 truths verified
**Gap-Closure Score (Plans 04 + 05):** 2/2 truths verified
**Total Score:** 29/29 truths verified.

### Required Artifacts (carried from initial verification — 15 + 2 new = 17 all VERIFIED)

15 initial artifacts unchanged. 2 new artifacts from gap closure:

| Artifact                                                  | Expected                                                       | Status     | Details                                                                                                                                  |
| --------------------------------------------------------- | -------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `app/templates/partials/library_stats.html` (modified)    | hx-trigger="every 10s" (no `load,` prefix)                     | ✓ VERIFIED | Line 3 confirmed via grep; 23 other lines preserved verbatim                                                                              |
| `tests/test_library_stats_partial.py` (new file)          | TestLibraryStatsPartialPolling with 4 regression tests + local client fixture | ✓ VERIFIED | 135 lines, 1 class, 4 tests (all pass); local fixture at lines 29-48 (NOT promoted to conftest.py per plan scope); 4 passed in 1.96s |

Plus suggestions_discovery.py + tests/test_suggestions_discovery.py both updated in-place (already counted in initial 15 artifacts; substantive content additions verified above).

### Key Link Verification

All 13 initial key links: ✓ WIRED (unchanged). Plus 8 new gap-closure links: ✓ WIRED (see GAP-01 + GAP-02 sections above).

### Anti-Patterns Found

Same 2 Info-level findings as initial verification (Phase 7 Plan 01 _create_plex_suggestions_playlist docstring + Phase 7.1 fake-client comment). **No new anti-patterns introduced by Plans 04 + 05.**

### Code Review Findings

Same 4 warnings from 07.1-REVIEW.md as initial verification — none block goal achievement, all out of scope for gap closure.

### Pre-Existing Test Failures (Deferred)

3 pre-existing `tests/test_sync_scheduler.py` failures (already documented in initial verification + deferred-items.md). NOT touched by Plans 04 + 05. Confirmed unchanged.

### Human Verification Required (4 items — pending NAS UAT)

All 4 items from initial verification remain pending. **GAP closure status:**

1. **Settings page cost meter renders weekly framing** — GAP-02 closure removes the 25-50 req/s ambient NAS load that contaminated this UAT. Now cleanly testable.
2. **First-deploy startup catch-up fires within 10 seconds** — GAP-01 closure caps the prompt at ~150K tokens via two complementary controls. **Now unblocked at the code level.** Pending NAS re-deploy.
3. **Sunday 03:00 UTC cron actually fires** — time-based; no code changes.
4. **Every TrackPlay webhook refills mirror via SQL with $0 LLM cost** — no code changes since main 07.1 plans.

### Gaps Summary

**No remaining gaps.** All 29 must_haves verified against the live codebase. Both UAT-surfaced gaps (GAP-01 + GAP-02) are CLOSED in code via Plans 04 + 05. Live test execution confirms:
- 11 new GAP-01 tests pass (TestDiscoveryEligible + TestDiscoveryPromptSize)
- 4 new GAP-02 tests pass (TestLibraryStatsPartialPolling)
- 21 Phase 7.1 invariant tests preserved (TestSuggestionsDiscoveryAstShape + TestSuggestionsDiscoveryMaxTokensGuard + TestDiscoveryCallWeekly + TestNoLegacyLlmRefillRefs)
- 5 existing partial-related tests preserved (TestLibraryAtAGlanceRender + TestStatsEndpoint)
- 45 Phase 7.1 cross-module tests preserved (event_handlers + anthropic_client + llm_cost_breaker + pages_settings)

The remaining work is **4 NAS UAT items** — none blocked by code gaps. Items #1 and #2 have been **unblocked** by Plans 04 + 05 closures and can now be re-attempted on the NAS after redeploy.

---

_Initial verification: 2026-05-15T22:59:43Z_
_Re-verification (gap closure): 2026-05-16T09:15:00Z_
_Verifier: Claude (gsd-verifier)_
